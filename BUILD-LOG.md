# DIVERGENCE.AI BUILD LOG

## WHERE YOU ARE
Last completed: STEP 5.0 — Input composer and control row (done. STORE ADD: SessionState.draftInput: string (default "") — the composer's not-yet-submitted textarea content, added to SESSION_PERSISTED_KEYS so the Step 1.8 autosave covers it with zero new persistence code; STORE-CONTRACT.md updated. New src/services/composer.ts: TranslateAskRequest type + buildTranslateAskRequest() — the exact typed object TRANSLATE & ASK emits (rawInput, model, directness, techniques, context), no API/translate/route call anywhere in it. New src/components/composer/: InputBox [auto-grows via JS scrollHeight up to 480px then internal-scrolls, "0 / 2000" counter is informational-only and never blocks typing, first real consumer of Step 1.2's unused --font-size/weight-section-header tokens], AttachContextControls + TranslateAskButton [real focusable buttons; Attach/Context emit events only, no file handling — that's Phase 7], ControlRow [arranges the ALREADY-BUILT Model/Directness/Technique dropdowns, does not rebuild them], Composer [the real, reusable deliverable — emits via onSubmit, clears draftInput on submit], ComposerSection [temporary demo wrapper showing the last emitted request as JSON, since Step 5.2's orchestrator doesn't exist yet]. REMOVED the three now-superseded Step 3.2/4.4/4.5 proof-of-wiring demo cards (ModelRoutingDemo/DirectnessDemo/TechniqueSelectionDemo) — their real dropdown components now live in ControlRow instead, so the standalone demos would just be confusing duplicates; ConversationArea (Step 2.3) is untouched. Verified: kill-and-reload test extended with a mid-sentence draftInput scenario (the step's own explicit "reload mid-sentence, confirm the text comes back" VERIFY, made concrete rather than asserted in prose) + 4 new composer.test.ts tests (incl. that huge/empty input passes through unclipped) — suite now 157 passing — clean tsc+vite build, clean oxlint. Honest screenshot diff logged in DECISIONS — several things are NOT built here (state detection panel between input and controls, all icons) and that's correct per this step's own scope boundary, not an oversight)
Last completed: STEP 5.1 — Streaming display (done. STORE ADD: ConversationMessage gained optional confidence/downgraded/notes (assistant-only) — Step 5.1 was named as this type's owning step in STORE-CONTRACT.md; a `rating` field was deliberately NOT added, left to Step 8.1. New src/services/answerDisplay.ts: AnswerDisplayState union [stage|streaming|done|error], PIPELINE_STAGES=[translating,routing,composing,answering] verbatim from the step's own text, streamAnswer() turning an AsyncIterable<string> (e.g. Step 1.10's proxyClient.stream()) into growing-text states then one done state carrying confidence+downgraded+notes. New src/components/streaming/: MessageBubble [screenshot layout: avatar/name/timestamp/content], AnswerMeta [confidence line + router honesty notes, same quiet monochrome], RatingRow [real 5-star + download buttons, events only — Step 8.1/8.5 own save/format behavior], StageIndicator, StreamingAnswer [renders whichever state], useAnswerDisplay [drives an AsyncGenerator into React state]. Replaced Step 2.3's three sample gate-state cards in ConversationArea with real session.conversation rendering (empty until 5.2) + StreamingAnswerDemo (offline stage/token simulation proving the whole machine end to end — gate.ts/TranslationCard/ClarifyPrompt untouched, still fully tested, relevant again once 5.2 wires the clarify path). Verified by 4 new answerDisplay tests [strictly-growing streaming states, router-honesty fields carried verbatim, empty-stream edge case] — suite now 161 passing — clean tsc+vite build [100 modules], clean oxlint)
Last completed: STEP 5.2 — Pipeline orchestrator (done. New src/services/pipeline/: runPipeline() as an AsyncGenerator<PipelineEvent> — composer request → translate() → gateTranslation → route() → technique selection → composeFinalPrompt() → proxyClient.stream, every stage's output yielded as a typed event (the sequence IS the stage log), deps injected (client/plan/AbortSignal), zero store imports, nothing rebuilt in any stage. New src/components/pipeline/: usePipelineRun (drives events into React state, console.info-logs each event) + CenterColumn (the live center column — real Composer wired to the real pipeline; raw input appended as a user message, the finished answer appended as an assistant message persisting confidence/downgraded/notes via the Step 1.8 autosave; the <60 clarify path finally renders Step 2.3's TranslationCard/ClarifyPrompt live, refine re-runs the whole pipeline on original+addition). ComposerSection/StreamingAnswerDemo/streamingDemo.ts deleted as superseded demo scaffolding. PIPELINE-CONTRACT.md written: every handoff, the gates, the coupling, the event vocabulary, which fields may be absent. Verified: 10 offline orchestrator tests where ONLY the model client is stubbed (routing.js, auto-detect, composition all run real — suite now 171 passing, clean tsc+vite build, clean oxlint) + the step's required 5 fresh-context subagent audits of contract vs code, one per stage: 4 clean passes, 1 real finding (the manual technique path didn't re-enforce CANON's ≤4 stack cap against a corrupted persisted array) fixed in code with a test, plus one contract phrasing sharpened. The "real question through the real deployed proxy" VERIFY is honestly UNRUN — no network/key/deploy in this sandbox, same standing residual as every step since 1.10, parked for 12.3.)
Last completed: STEP 5.3 — Error, retry, timeout (done. New src/services/pipeline/resilience.ts: TimeoutError, isTransientError (network TypeError/timeout/429/5xx retryable; other 4xx and unrecognized errors not), withTimeout (per-attempt abort via a merged AbortSignal), withResilience (exponential-backoff retry, real cancellation via deps.signal never retried), makeResilientClient (wraps a PipelineModelClient's complete/stream with both — stream retries ONLY before the first chunk is yielded, never after, so a mid-generation failure can't duplicate output the user already saw). orchestrator.ts: runPipeline now wraps deps.client internally with makeResilientClient (callers pass the raw client; resilience is guaranteed by construction, not left to the caller to remember) and wraps EVERY stage's call in its own try/catch yielding a terminal {kind:"error"} event on any exception — runPipeline itself now never throws, extending translate()'s Step 2.2 guarantee to the whole pipeline. New PipelineDeps.resilience?: ResilienceOptions for tuning/tests (production uses the defaults: 3 attempts, 300ms backoff doubling, 30s per-attempt timeout). No UI changes needed — the existing stage indicator (visible from time zero of any wait) already satisfies "indicator for any wait over 1 second," and the existing neutral fixed-copy error rendering (TranslationCard/StreamingAnswer, Steps 2.3/5.1) already satisfies "neutral non-blaming copy on failure." PIPELINE-CONTRACT.md gained a "Resilience" section. Verified: SPOT CHECK confirmed CANON.md's time rules present (300ms/1s-indicator/no-countdown/no-blame, CANON.md:73,81) — 15 new resilience.test.ts tests (transient-retry-then-succeed, non-transient-no-retry, retries-exhausted, real-cancellation-never-retried, timeout-is-graceful, stream-retry-before-first-chunk, stream-never-retries-after-first-chunk) + 4 new orchestrator-level integration tests (translation-call transient retry proceeds normally, translation-call timeout ends gracefully at the clarify-style error card, execution 502 still retries then fails cleanly, execution 400 does not retry) — suite now 189 passing, clean tsc+vite build, clean oxlint. Also fixed a housekeeping miss from last session: Step 5.2's own STEPS checkbox had never been ticked despite being logged done — ticked now, along with 5.3's.)
Last completed: STEP 5.4 — Telemetry (done. New src/services/telemetry/: types.ts [TelemetryEntry — id/timings/complexity/model/techniques/confidence/notes/translationTokens+executionTokens/outcome/errorMessage, exactly the fields the step names, one entry per pipeline request], log.ts [in-memory bounded ring buffer, MAX_TELEMETRY_ENTRIES=200, NOT a third Zustand store — recordTelemetryEntry/getTelemetryEntries/subscribeTelemetry/resetTelemetryForTests], observePipeline.ts [a transparent tee around runPipeline — yields the IDENTICAL PipelineEvent sequence, unedited, while timing each stage and pulling complexity/model/techniques/confidence/notes out of the same events the UI renders, and recording a finalized entry at whichever terminal event ends the run, incl. a "cancelled" outcome via the tee's own finally when a consumer stops iterating early]. Extended src/services/proxyClient.ts (Step 1.10) additively: new optional ProxyCompletionRequest.onUsage callback, complete() reports it from the JSON response's usage field, stream() reports it once from SSE message_start/message_delta events — return shapes of complete()/stream() are completely unchanged, translate.ts's Step 2.2 seam untouched. CenterColumn.tsx now calls observePipeline instead of runPipeline (one-line swap; everything else about its wiring is untouched). SPOT CHECK confirmed PIPELINE.md's Stage 5 explicitly names "log telemetry" feeding the learning loop, and CANON Feature 8's Transparency card fields (routing/techniques/confidence) are exactly what telemetry now captures — both future consumers (Token Usage panel Step 9.6, Learning Loop Step 10.1, Transparency card Step 8.2) don't exist yet, so nothing reads the log outside tests. PIPELINE-CONTRACT.md gained a "Telemetry" section. Verified: 6 new observePipeline.test.ts tests (transparent-tee event-sequence equality, successful-run full field capture incl. both token counts and all 4 stage timings, two sequential runs don't cross-contaminate, clarify path captures confidence but not routing fields, execution failure still records what happened before the failure, cancellation records outcome "cancelled") + 4 new proxyClient.test.ts tests (onUsage fires from complete()'s JSON response, fires from stream()'s SSE events, and is never called when no usage data is present, both directions) — suite now 199 passing, clean tsc+vite build, clean oxlint.)
Last completed: STEP 6.1 — Detection architecture (done. SPOT CHECK confirmed PIPELINE.md STATE DETECTION (line 40) says detection fires ON THE BUTTON alongside translation, NOT live while typing, no local heuristic tier, no sub-300ms budget — the RESOLVED single on-demand classification, not the retired two-tier approach. New src/services/detection/ mirroring the translation service's proven 3-part structure: schema.ts [StateDetectionResult — each of the four dimensions (emotion/rsd/interest/cognitive) is a DimensionReading{value,confidence}|null, plus a user-facing summary; the four value unions are REUSED from stores/types.ts (EmotionState/RsdLevel/InterestLevel/CognitiveMode, Step 1.7) so pills and result speak one vocabulary; tolerant parseDetectionOutput (unknown value → that dimension null, no fabricated neutral; garbage confidence → 0; throws DetectionSchemaError ONLY on a non-object reply — detection has NO single load-bearing field, unlike translation's translatedPrompt); toStatePills() projects the 4 values onto the store's StatePills], client.ts [DetectionModelClient seam + DETECTION_MODEL="claude-haiku-4-5" typed as ModelId, structurally identical to translation's seam so proxyClient.complete satisfies both], prompt.ts [DETECTION_SYSTEM_PROMPT — a working classifier; Step 6.2 OWNS finalizing/tuning it], detect.ts [detectState() service — never throws, typed DetectionOutcome union {ok|empty|too-large|error}, mirrors translate() field-for-field since the two run TOGETHER on the same input on the same button press; shares the 24k input cap]. Service is standalone + stub-tested, NOT yet wired into the pipeline/UI (6.3 pills UI, 6.5 state feeds consume it) — same "service built, then wired" pattern as translation 2.2→5.2. Verified: 17 new tests (schema.test.ts 10 — tolerant per-dimension parsing, null-not-fabricated, non-object throws, all-null OK, toStatePills projection; detect.test.ts 7 — happy path calls haiku with the prompt+trimmed input, prose/fence tolerance, empty/too-large short-circuit without calling model, transport+unparseable → error not throw) — full suite 216 passing (26 files), clean tsc+vite build, clean oxlint. NOTE on verify: a first full-suite run showed "8 errors / 0 failures / 149 passed / 18 files" — diagnosed as vitest worker-pool crashes under memory pressure (it ran concurrently with build+lint); a clean isolated re-run gave 216/216, 0 errors. Detection service is tree-shaken from the bundle for now (no consumer yet), expected.)
Last completed: STEP 6.2 — Four-dimension classifier prompt (done. SPOT CHECK confirmed the four dimensions AND each state's system impact match PIPELINE.md lines 44-50 (Emotion: Overwhelmed→directness L1/Frustrated→Simplify/Calm→Socratic/Excited→Detailed/Anxious→Verify; RSD: High→warm/Medium→balanced/Low→direct; Interest: Low→Simplify/Medium→standard/High→Detailed-or-Comparative; Cognitive: Analytical→CoT-or-Step/Creative→Metaphor-or-Comparative/Processing→Socratic/Racing→Simplify/Stuck→Examples). Rewrote src/services/detection/prompt.ts's DETECTION_SYSTEM_PROMPT — finalized + tuned for claude-haiku-4-5 (short, one instruction per line, explicit output shape, "JSON only, do not reason out loud" since a fast classifier should emit directly), and every state now names its SYSTEM IMPACT so the model's summary can tell the user what will change. New src/services/detection/impacts.ts: the AUTHORITATIVE STATE_IMPACTS table (each dimension value → StateImpact{description, directness?, techniques?, tone?}) — impacts are DERIVED DETERMINISTICALLY from the detected state values (impactsFor/recommendedDirectness/suggestedTechniques pure helpers), NOT emitted by the model, so they're auditable and can't be hallucinated (same deterministic-where-possible philosophy as routing.js). "X or Y" impacts (High-interest, Analytical, Creative) record BOTH as candidate techniques — Step 6.5 reconciles via auto-detect's conflict/dep/≤4 machinery, does not blind-stack. New STATE-DETECTION-SPEC.md documents dimensions/signals/impacts/prompt/schema (code wins on drift, mirrors TRANSLATION-SPEC.md). Schema unchanged from 6.1 (already final; 6.2 confirms + documents it). Verified: 17 new impacts.test.ts tests (table covers all 16 values, techniques are valid TechniqueIds, Overwhelmed-only-directness-L1, every specific PIPELINE mapping asserted, deterministic derivation, recommendedDirectness AGREES with directness.ts's recommendDirectnessFromEmotion across all emotions, suggestedTechniques de-dups + drops RSD, prompt names every state + Haiku-tuning present) — full suite 233 passing (27 files), clean tsc+vite build, clean oxlint. Real haiku accuracy still UNMEASURED (no-network sandbox; parked for 12.3).)
Last completed: STEP 6.3 — State pills UI (done. SPOT CHECK confirmed MATERIALS.md explicitly allows purple on Emotion ("Purple #8B5CF6 for the logo 'AI' text AND for state pills (Emotion pill is purple, this is intended)", line 34) — no conflict to resolve, all four screenshot colors (--state-emotion/rsd/interest/cognitive, set at Step 1.2) reused as-is. New src/components/detection/: pillOptions.ts [PILL_CONFIGS — dimension/label/colorVar/values, reusing services/detection's EMOTION_STATES etc. arrays directly by reference, no second taxonomy], StatePill.tsx [reuses the Step 1.4 Pill primitive; "Label: Value" format matching the screenshot; a sibling × dismiss button, not nested-in-button], PillCorrector.tsx [the "small correction control" — offers the dimension's other valid values as buttons, no useFocusTrap since "Adjust" can open all four correctors at once and trapping 4 simultaneous regions isn't sensible], StateDetectionPanel.tsx [matches the screenshot: header with info glyph + panel-level ×, pills row, summary line, Adjust button that opens/closes ALL visible correctors at once; per-pill dismiss + correction ALSO built per PIPELINE.md Feature 5's own text ("Pills are dismissible (X) and correctable"), which is more specific than the screenshot's single visible × — same "explicit written spec over an ambiguous/silent screenshot" precedent as Step 1.5]. WIRED FOR REAL: CenterColumn.tsx now fires detectState() ALONGSIDE observePipeline() on every submit AND on refine (both go through the shared startRun, so detection re-fires free on refine too) — sharing the submission's AbortController (a resubmit cancels both), NOT added as an orchestrator stage (detection isn't one of PIPELINE.md's five stages; orchestrator.ts stays untouched, its 3rd protected file in a row). A successful result feeds the panel's local rich state (confidence/summary, for display+correction) AND session.statePills (Step 1.7's existing field — the durable value 6.5 will deliver to its four consumers). Composer.tsx renders <StateDetectionPanel/> between <InputBox/> and <ControlRow/>, exactly the position the Step 5.0 PARKED note named. Corrections (6.3's job: make visible immediately) update the local+store state; RECORDING corrections for the 15+ threshold learning loop is explicitly left to Step 6.4. New src/styles/detection.css. Verified: 6 new pillOptions.test.ts tests (dimension order/labels/colors match the screenshot, values ARE the same array references as services/detection — not copied, capitalizeValue) — same repo-wide no-RTL choice as every prior dropdown/popover step (Steps 3.2/4.4/4.5), components typecheck+build verified only. Full suite 239 passing (28 files), clean tsc+vite build [124 modules, up from 112 — detection is no longer tree-shaken out], clean oxlint. No real browser in this sandbox (standing constraint since Step 1.1) — pill colors/layout/popover positioning unverified against an actual render.)
Next step: STEP 6.4 — Correction learning
Blocked: nothing. Only the proxy's live deploy + real streamed call (and therefore a real corpus accuracy run) remain unrun in this no-network sandbox (parked for deploy, Step 12.3).

## DECISIONS
- Locked decisions verified: three-marble system (not seven), claude-sonnet-5 for Balanced tier, state detection on button press (not live-as-you-type), routing engine already built (not rebuilt).
- Routing engine string change identified: routing.js needs claude-sonnet-5 update in MODELS table (Step 2.2 applies this).
- Texture files location clarified as needed before Step 3.1 (three files: one black marble, two greys).
- Translation test corpus (31_0_translation_test_cases.md) marked as optional for Step 2.4; not blocking current progress.
- STACK LOCKED (Step 1.1): React 19 + TypeScript + Vite 8 SPA; Zustand for the two CANON stores; IndexedDB via `idb` (CANON mandates IndexedDB by name); vanilla CSS custom properties for the marble/glass system (Tailwind and CSS-in-JS rejected); Tauri for Windows wrap, Capacitor for Android. Svelte 5 and SolidJS seriously considered, rejected because 58 remaining blind sessions make ecosystem predictability worth more than marginal reactivity gains. Full reasoning in STACK.md.
- Frame implemented as CSS grid (`grid-template-areas`), not nested flex: one declaration encodes all four locked dimensions (60/200/flex/300), harder for a later step to break one region in isolation. Regions carry data-testids: topbar, col-left, col-center, col-right.
- Layout dimensions live as CSS custom properties in src/styles/layout.css (--topbar-height, --left-col-width, --right-col-width); Step 1.2 tokens should extend this pattern in src/styles/tokens.css (created empty as a landing point).
- Vite 8 template ships oxlint (not eslint) — kept as-is rather than swapping.
- FONT (Step 1.2): Anthropic Sans (named in MATERIALS.md) is not publicly licensed and cannot ship. Substituted Inter (SIL OFL, freely shippable) as --font-family-base — closest clean humanist-geometric sans at the same register, and the de facto standard at the 12-14px sizes this whole type scale runs at. Only the stack is fixed here; loading/self-hosting the actual font file is Step 3.4's job, not this one.
- COLOR VALUES NOT GIVEN AS EXACT HEX (Step 1.2): MATERIALS.md/CANON.md give exact hex only for Black Marble, Blue Marble, cyan (#00D9FF), and purple (#8B5CF6). RSD, Interest, and Cognitive Mode pill colors are named only by hue ("red", "green", "blue"). Picked --state-rsd/--action-destructive #EF4444, --state-interest #22C55E from the screenshot by eye. --state-cognitive #3B9EFF deliberately kept distinct from Blue Marble's #2B4E9C-#4478E5 range so a state pill never visually reads as a button. --state-emotion aliases directly to --accent-purple (#8B5CF6) per MATERIALS.md's explicit statement they're the same purple. A later session with real pixel-sampling tools should verify these three against the screenshot and adjust the token values only (no component touches these directly).
- RADIUS (Step 1.2): MATERIALS.md gives cards a 12-16px range, buttons a flat 8px. --radius-button is 8px (unambiguous). --radius-card is fixed at 14px, the range's midpoint, as a single token a later step can retune without touching any component.
- TOKEN FILE ARCHITECTURE (Step 1.2): flat single-tier custom properties in tokens.css, no primitive/alias indirection layer. Simpler than a two-tier system and CONVENTIONS.md only asks for domain-prefixed semantic names, not a primitive layer — revisit only if token reuse patterns actually demand it later.
- SWATCH PAGE (Step 1.2): public/token-swatch.html, a static HTML file referencing /src/styles/tokens.css directly. Dev-only, not linked from AppShell or main.tsx, not part of the shipped bundle — exists purely so a session with a real browser can open it and eyeball every token at once against the screenshot.
- FILE-MANIFEST.md CORRECTION: an earlier version of this line claimed Step 0's manifest "was never written or saved anywhere in the project tree." That was wrong. The real Step 0 manifest genuinely existed — it just wasn't saved into this project folder at the time, so Step 1.3's REQUIRED FILES check couldn't find it and wrote its own stand-in under the mistaken belief that none had ever existed. The real Step 0 manifest has since surfaced and now lives at FILE-MANIFEST.md in the repo root — the authoritative Step 0 deliverable. Step 1.3's stand-in was renamed to FILE-MANIFEST-STEP1.3-ADDENDUM.md rather than deleted, because its texture-file findings (source filenames, the .jpg-extension-but-actually-PNG correction, the near-identical-textures flag, the seam-tiling measurement) were discovered during Step 1.3 itself, postdate the real Step 0 manifest, and are still accurate and still in use — see PARKED and MARBLE-CONTRACT.md.
- TEXTURE FILES DO NOT TILE SEAMLESSLY AS PROVIDED (Step 1.3): the three raw texture files are 1254x1254px. A prior investigation into these same three source files (MAX Effort - Marble Design Output / MARBLE-SLAB-ENFORCEMENT-CONTRACT.md — confirmed same files by matching 1254x1254 dimensions and matching descriptive names) measured that raw textures at this resolution do not tile seamlessly (wrap discontinuity 1.6x-2.9x baseline), which reads as a visible seam on background-repeat. That prior work already built the fix: seamless 2508x2508 mirrored supertiles (measured wrap discontinuity 0.00). Per explicit instruction, those pre-built supertiles were copied into this repo as `public/textures/black-marble-slab.jpg`, `grey-marble-slab-1.jpg`, `grey-marble-slab-2.jpg` and are what marble.css actually references as background-image. The three raw originals (`black-marble.png`, `grey-marble-1.png`, `grey-marble-2.png`, saved at Step 1.3) are kept only as canonical source of record per FILE-MANIFEST-STEP1.3-ADDENDUM.md — no component loads them. Full detail in MARBLE-CONTRACT.md and FILE-MANIFEST-STEP1.3-ADDENDUM.md (not the root FILE-MANIFEST.md, which is the real Step 0 deliverable and predates these textures being supplied).
- PRIOR-ART FOLDERS ARE UNVERIFIED UNTIL CONFIRMED (Step 1.3, explicit instruction for all future steps): the "Fable - MAX Effort - Marble Design Output" folder turned out to contain a directly relevant, verified prior investigation (used at Step 1.3, above), but that was confirmed with the person running this build before being used, not assumed. Any future step that finds other pre-existing output in that directory (or elsewhere outside the repo) must not use it on its own judgment — flag it and ask first, the same way Step 1.3 did.
- SLAB ARCHITECTURE (Step 1.3): one `.marble-slab` element (`position: fixed; inset: 0; z-index: 0`), mounted once in main.tsx as a sibling to `<AppShell />` (never nested inside it, so no ancestor can pick up a transform/filter/backdrop-filter that would silently break `position: fixed`). All app content wrapped in `.app-layer` (`position: relative; z-index: 1`) — currently just AppShell's root div. Technique ported directly from MARBLE-SLAB-ENFORCEMENT-CONTRACT.md's proven architecture (that prior work explicitly found `background-attachment: fixed` broken on iOS Safari and repaint-costly, using `position: fixed` on the slab instead — adopted here for the same reason).
- BLUE MARBLE BUTTON DEVIATES FROM PRIOR CONTRACT, DELIBERATELY (Step 1.3): the prior MARBLE-SLAB-ENFORCEMENT-CONTRACT.md banned marble texture on every component, no exceptions, and specifically forbade `background-attachment: fixed` anywhere. Current MATERIALS.md (CANON-authoritative, postdates that prior work) requires Blue Marble buttons to sample marble "from the same slab coordinates as the background" as a deliberate sole exception. `.surface-blue-marble` (marble.css) satisfies this using `background-attachment: fixed` on its marble layer ONLY (not on the slab itself, which still uses `position: fixed` per the prior contract's proven technique) — a narrower, bounded use case the prior contract never had to rule on, since its source design had no textured buttons at all. Chosen over per-button JavaScript position tuning, which the prior contract correctly identifies as fragile (drifts on any layout change). Full reasoning in MARBLE-CONTRACT.md "DEVIATION FROM PRIOR ART."
- GREY SUPERTILES SAVED BUT UNUSED (Step 1.3): `grey-marble-slab-1.jpg` and `grey-marble-slab-2.jpg` copied in alongside the black one for completeness, but no component references them — CANON's three-surface system (Black Marble, Smoked Glass, Blue Marble) has no defined use for a fourth "grey marble" surface. Reserved, not wired to anything, matching the prior contract's own treatment of the same asset ("ships unused so no future agent rebuilds it differently").
- BOTTOM SLAB BAND (Step 1.3): `.app-shell` height changed from `100vh` to `calc(100vh - var(--space-section-margin))` (24px) so a full-width strip of slab is visible below the three-column frame, per MATERIALS.md's slab-rule enforceable check. Grid rows/columns/areas and all locked dimensions (60/200/300) are unchanged — only the container's total height.
- TOKENS.CSS WAS NEVER ACTUALLY IMPORTED INTO THE APP (found and fixed at Step 1.3): Step 1.2 built tokens.css and linked it from the standalone swatch page, but main.tsx never imported it — harmless while AppShell was empty (Step 1.1), but marble.css's var() references would have silently resolved to nothing once real CSS started consuming them. Added `import "./styles/tokens.css"` to main.tsx, before layout.css and marble.css.
- LAYOUT.CSS HARDCODED VALUES CLEANED UP (Step 1.3): body's placeholder `background: #111; color: #eee; font-family: system-ui` and the three region borders' hardcoded `#333` replaced with token references (`--text-primary`, `--font-family-base`, `--surface-smoked-glass-border`) now that tokens.css is actually wired in. body's own background removed entirely — `.marble-slab` is a fixed full-viewport layer that fully covers it.
- SELF-CHECK METHOD (Step 1.3): per the step's own instruction, verification against MATERIALS.md's slab rule was done by a fresh-context Explore subagent given MATERIALS.md, MARBLE-CONTRACT.md, and the actual source files (not my reasoning about them) with instructions to statically audit for rule violations. All checks passed — see PARKED for what this does and does not cover (no real browser, so no literal resize/pixel test).
- PRIMITIVES REUSE STEP 1.3 MATERIAL, DO NOT RE-DECIDE IT (Step 1.4): GlassCard/GlassPanel use `.surface-smoked-glass` and BlueMarbleButton uses `.surface-blue-marble` exactly as built in Step 1.3 (marble.css) — unchanged. New CSS (primitives.css) only adds layout/sizing classes (`.primitive-glass-card`, `.primitive-glass-panel`, `.primitive-glass-button`) composed alongside the existing surface classes via multiple classNames, not by duplicating the material declarations.
- NEW `src/components/primitives/` FOLDER (Step 1.4): CONVENTIONS.md's repo-layout tree only shows `components/layout/` and `components/<feature>/` explicitly, no primitives category. Added anyway: primitives are cross-cutting (used by every future feature), analogous to `layout/` as a structural category rather than a single feature, and CONVENTIONS.md's own tree is illustrative ("one folder per CANON feature as it's built, e.g. ...") not exhaustive. Logged here per the same transparency principle as an unlisted dependency addition (CONVENTIONS.md rule 3) — flag it plainly rather than assume it's uncontroversial.
- GLASSBUTTON AND DROPDOWN GET A 44PX MINIMUM CLICK TARGET (Step 1.4): MATERIALS.md states 44px minimum only for Blue Marble buttons explicitly. Extended the same floor to GlassButton and Dropdown's `<select>` — CANON's ADHD hard rules ("Accessibility") apply generally, not just to the one surface MATERIALS.md happened to spell it out for.
- DROPDOWN IS A STYLED NATIVE `<select>` (Step 1.4): not a custom ARIA combobox/listbox. Gets keyboard operation (arrows, type-ahead, Enter, Escape) for free, satisfying "keyboard-focusable with a visible focus ring" without building custom ARIA at the primitive-library stage. A feature step needing a richer listbox (icons, multi-column, live-filtering) builds on top of this rather than this primitive being replaced.
- PILL IS GENERIC, NOT STATE-SPECIFIC (Step 1.4): `Pill` takes a caller-supplied `color` prop (a CSS color value, e.g. `var(--state-emotion)`), applied via a `--pill-color` custom property — it has no built-in knowledge of Emotion/RSD/Interest/Cognitive Mode. Step 6.3 (State pills UI) is what gives pills that meaning; this primitive only provides the shared visual shape. Renders as `<button>` (real tab stop) only when `onClick` is supplied, otherwise `<span>`, so decorative pills don't consume keyboard focus.
- FOCUS RING REUSES `--accent-cyan`, NO NEW TOKEN (Step 1.4): every primitive gets `outline: 2px solid var(--accent-cyan); outline-offset: 2px` on `:focus-visible` (primitives.css), satisfying the step's "visible focus ring" requirement and CANON's ADHD hard rule on the same. Reused the existing cyan accent token rather than adding a dedicated `--focus-ring-color` token — one fewer token for the same value, revisit only if a future step needs the focus color to diverge from cyan.
- AppShell.tsx's DEMO CONTENT REPLACED, NOT DUPLICATED (Step 1.4): Step 1.3's raw `surface-smoked-glass`/`surface-blue-marble` demo divs in AppShell.tsx are now built from the six real primitives instead. This doubles as the step's required "page rendering every primitive over the slab" deliverable — no separate showcase page was built, since AppShell already renders inside `.app-layer` over `.marble-slab`. Still explicitly demo content, not real feature UI; Step 1.5+ replaces it.
- TOPBAR AND LEFTNAV ARE REAL, NOT DEMO (Step 1.5): new `TopBar.tsx` and `LeftNav.tsx` in `components/layout/` replace all Step 1.3/1.4 demo content in AppShell.tsx entirely (no Pill/Dropdown/BlueMarbleButton demo usage left). Both reuse the Step 1.4 primitives (`GlassButton`) rather than inventing one-off nav-item styles.
- NO ICON LIBRARY YET; TEXT-LABEL PLACEHOLDERS (Step 1.5): the logo slot, Settings/Notifications/Help buttons, and all ten left-nav items render as plain text, not icons — STACK.md doesn't include an icon library and adding one is a stack decision outside this step's scope (CONVENTIONS.md rule 3: "Adding a dependency is a DECISIONS line"). Deferred to whichever step actually wires real icons (likely Step 1.6 for the logo specifically, and a polish/component step for the rest).
- CENTER COLUMN IS A BARE TEXT PLACEHOLDER, NOT A SCREENSHOT REPLICA (Step 1.5): the V3 screenshot shows a fully populated conversation (messages, input box, state pills, dropdowns, TRANSLATE & ASK). Step 1.5's own numbered scope explicitly says "Center column flex: empty conversation placeholder" — treated that as more specific than the general "match the screenshot exactly" instruction, since the populated conversation UI is explicitly built incrementally across Steps 5.0+ and 6.x. Center column here is one placeholder `<p>`, nothing else.
- RIGHT COLUMN IS TWO MINIMAL PLACEHOLDERS, NOT THE REAL GRID/ACCORDION (Step 1.5): built two `GlassPanel`s with placeholder text (Quick Tools; the six accordion section names in one sentence) rather than the actual 2x3 Quick Tools grid or six real accordion headers. Reasons: (1) Step 1.5's own scope says "placeholders," (2) CANON's locked decision "Quick Tools panel defaults to hidden (OFF)" has no meaningful toggle to be OFF from until Step 9.4 (Visibility toggle) exists — rendering a fully-built, always-visible grid now would contradict that decision. Steps 9.4/9.5/9.6 own the real versions.
- LOGOUT CONTROL ADDED DESPITE NOT BEING VISUALLY DISTINCT IN THE SCREENSHOT (Step 1.5): CANON.md's LOCKED DECISIONS explicitly states "logout button bottom-left under System Status," and Step 1.5's own numbered list names it, even though the V3 screenshot doesn't show separate visible logout text at that position. Followed the explicit written spec over an ambiguous screenshot read, per CANON.md's own precedence rule only applying when text and image actually disagree (here the image is just ambiguous/silent, not contradicting).
- NEW `--status-online` TOKEN (Step 1.5): `#22c55e`, same value as `--state-interest` — MATERIALS.md's palette only defines one green — but a separate named token, since System Status (health/connectivity) and Interest (a detected emotional-state dimension) are unrelated concepts that could diverge later even though they start identical.
- NEW `src/styles/shell.css` FILE (Step 1.5): structural layout for content *within* the topbar/left-nav regions (flex arrangement, gaps, the system-status row). Kept separate from `layout.css`, which CONVENTIONS.md scopes to "the structural frame ONLY (grid, dimensions)" — shell.css is the next layer in, parallel to how Step 1.3 got marble.css and Step 1.4 got primitives.css.
- LEFTNAV ITEMS ARE INERT (Step 1.5): the ten nav buttons and Trash/Logout have no click handlers, routing, or active-state styling (the screenshot shows "Home" highlighted as active, not replicated here). Real navigation/active-state behavior is explicitly Step 9.7's job ("Left nav content"); Step 1.5 only places structure and spacing correctly.
- LOGO AURORA COLORS AS LOGO-SCOPED TOKENS (Step 1.6): CANON names the aurora as "neon green, purple, pink blended" but gives exact hex for none of the three. Added `--logo-aurora-green` (#39ff14), `--logo-aurora-pink` (#ff4fd8), and `--logo-aurora-purple` (aliased to the existing `--accent-purple` #8b5cf6 so the "AI" text stays the app's one purple), plus a `--logo-aurora-gradient` composing them. Kept logo-scoped, not promoted to general accents — this three-color mix is the brand mark's identity, not a reusable UI accent. Green/pink hex are a confident pick from CANON's hue names; a session with the real brand source can retune the two token values without touching Logo.tsx.
- LOGO ANIMATION IS OPT-OUT, NOT ALWAYS-ON (Step 1.6): CANON's LOGO section calls for a "subtle synapse animation" while its sensory hard rule says "No excessive auto-animation." Reconciled by making the synapse pulse (slow 3.6s ease-in-out, opacity 0.4↔1, staggered per-node delays) run ONLY under `@media (prefers-reduced-motion: no-preference)` — reduced-motion users get a fully static logo. This is the first `prefers-reduced-motion` guard in the codebase; later animated steps should follow the same pattern (Step 11.2/11.3 audits will expect it).
- LOGO IS HAND-BUILT SVG, NOT AN ASSET FILE OR ICON LIBRARY (Step 1.6): the brain is an inline SVG path in Logo.tsx with a gradient stroke and pulsing `<circle>` synapse nodes, not a raster/exported asset and not from an icon set. Consistent with the Step 1.5 note that no icon library exists in this build; the logo specifically needs custom animation so it's bespoke regardless. The Vite template's placeholder public/favicon.svg and icons.svg are still unreplaced (see PARKED) — this step built the top-bar mark, not the favicon.
- LOGO.CSS IS CO-LOCATED, NOT GLOBAL (Step 1.6): unlike shell.css/primitives.css/marble.css (global stylesheets imported from main.tsx), Logo.css sits next to Logo.tsx and is imported by it. CONVENTIONS.md explicitly permits `ComponentName.css` next to a component; used here because the logo's keyframes and SVG-gradient wiring are genuinely component-specific, not shared vocabulary. First component-scoped stylesheet in the repo.
- ZUSTAND INSTALLED (Step 1.7): executes STACK.md's Step 1.1 pre-decision ("Zustand for the two CANON stores"). zustand ^5.0.14, compatible with React 19. `idb` deliberately NOT installed yet — IndexedDB persistence is Step 1.8's job, and Step 1.7 builds only the in-memory stores + schemas.
- STORE FIELD TYPES: TOP-LEVEL AUTHORITATIVE, NESTED PROVISIONAL (Step 1.7): the two store shapes (SessionState, AccountState) and their top-level fields are fixed here. Nested domain types a later step owns in full are minimal/provisional and marked so in types.ts and STORE-CONTRACT.md: ContextItem (Steps 7.x), ConversationMessage (5.x/8.x), StatePills (6.x), ArchivedPair (9.x), LearnedPreferences (10.x/6.4). Those steps may ADD fields but must not rename/remove the top-level store fields without updating STORE-CONTRACT.md. Enum-ish unions (ModelId, DirectnessLevel, TechniqueId, PlanFlag, state-pill values) mirror names already fixed in CANON/ROUTING.
- FACTORY FUNCTIONS, NOT SHARED CONST DEFAULTS (Step 1.7): createInitialSessionState()/createInitialAccountState() return fresh objects each call, used for both store init and reset. Chosen over a shared `const initialState` so arrays/objects can't be aliased across sessions (a reset can't leak a prior session's mutated array). Verified by a specific smoke-test assertion.
- *_PERSISTED_KEYS AS THE AUTOSAVE SEAM (Step 1.7): each store exports SESSION_PERSISTED_KEYS / ACCOUNT_PERSISTED_KEYS (the data field names, excluding actions) and a hydrate(partial) action. This is the seam Step 1.8's autosave layer uses to serialize/rehydrate without hardcoding field names or needing to distinguish action functions from data. Keeps 1.7 free of persistence code while giving 1.8 a clean contract.
- resetSession CLEARS SESSION STORE ONLY (Step 1.7): CANON says the session store is "cleared when a session closes"; the account store "persists across browser closes" and is never wiped by session close. So sessionStore has resetSession(); accountStore deliberately has NO reset-all action — only per-item mutations (removeSavedPrompt, removeVariable) remove individual entries.
- PLAN FLAG: FLAG NOT BILLING, DEFAULT FREE (Step 1.7): `plan: "free"|"paid"` lives in the account store, defaults "free" so routing.js's gated (free) path is exercised by default. Per ROUTING.md there is no payment/auth/account system — did NOT build billing, auth, or an upgrade flow. routing.js already accepts `plan` as input but isn't wired in yet (a later Phase-3 step); Step 1.7 only supplies the storage location.
- CONVENTIONS.md's "(Step 6.3)" STORE ANNOTATION IS STALE (Step 1.7): CONVENTIONS.md's repo-layout tree annotates the stores as built at "Step 6.3" — that's from the OLD plan numbering. The real plan (DIVERGENCE-BUILD-PROMPTS.md, the established authority for step ordering) builds them at Step 1.7. Followed the real plan; kept CONVENTIONS.md's file names (sessionStore.ts, accountStore.ts) and location (src/stores/), which are still correct. Also added src/stores/index.ts barrel and src/stores/types.ts (not individually enumerated in CONVENTIONS.md but consistent with its structure); removed the src/stores/.gitkeep placeholder now that the dir has real files.
- idb + TEST STACK INSTALLED (Step 1.8): `idb` ^8 (STACK.md's chosen IndexedDB wrapper) as a runtime dep; `vitest` + `happy-dom` + `fake-indexeddb` as dev deps. Step 1.8 explicitly requires a kill-and-reload test, so a runner was needed now — vitest (also what Step 12.1's unit tests will use), happy-dom for the window/document the autosave close-protection listeners need, fake-indexeddb for an in-memory IndexedDB in tests. Added `"test": "vitest run"` script, vitest.config.ts (env happy-dom, setup src/test/setup.ts loading fake-indexeddb/auto), and src/test/setup.ts. First test infrastructure in the repo.
- SINGLE ATOMIC TRANSACTION PER AUTOSAVE TICK (Step 1.8): saveNow() writes BOTH stores' persisted state in one IndexedDB readwrite transaction (object store "state", keys "session"/"account", DB "divergence-ai" v1). IndexedDB transactions are atomic, so a refresh/crash mid-write aborts the whole transaction and the last COMPLETE write remains — satisfies the "no corruption, last complete write wins" requirement by construction. Both stores in one tx also keeps them mutually consistent per tick. Verified by the "last complete write wins" test.
- CLOSE-PROTECTION FLUSH BEYOND THE 5s INTERVAL (Step 1.8): startAutosave() runs the 5s setInterval AND also flushes on `pagehide` and on `visibilitychange`→hidden, because CANON says "Crash and close protection" — a tab closed between ticks must not lose work. Returns a stop() that clears the interval and removes both listeners. The pagehide flush + stop() cleanup are unit-tested; the raw 5s timer firing is not directly tested (fake timers deadlock fake-indexeddb — see PARKED), but the interval is a trivial wrapper over the well-tested saveNow().
- RESTORE-BEFORE-RENDER IN main.tsx (Step 1.8): bootstrap() awaits loadPersistedState() BEFORE createRoot().render(), so the user never sees a default-then-restored flicker, then calls startAutosave(). The load is wrapped in try/catch so a blocked/failed IndexedDB (e.g. private browsing) still renders the app from defaults rather than white-screening. IDB read is milliseconds; if it ever costs perceptible startup time, switch to render-then-hydrate with a hydrating flag (see PARKED).
- PERSISTENCE USES THE 1.7 SEAM, HARDCODES NOTHING (Step 1.8): persistence.ts reads SESSION_PERSISTED_KEYS/ACCOUNT_PERSISTED_KEYS and calls each store's hydrate() — no store field names are hardcoded in the persistence layer, so a future step adding a field to a store + its *_PERSISTED_KEYS gets it persisted automatically. Type-only import of SessionState/AccountState from stores/types.ts (they aren't re-exported by name from the store files).
- NEW `src/keyboard/` MODULE + PURE-CORE/THIN-HOOK ARCHITECTURE (Step 1.9): keyboard framework lives in a new top-level `src/keyboard/` dir — added like `components/primitives/` (Step 1.4) because it's cross-cutting infra used by every future interactive component, not a single CANON feature; CONVENTIONS.md's tree is illustrative, logged here per rule 3 transparency. Each capability is a framework-agnostic core (isTypingTarget, dismissLayers, shortcutRegistry, focusTrap, confirmable) with a thin React-hook wrapper. Chose this split so the cores are unit-testable under happy-dom WITHOUT adding @testing-library/react — no new dependency (RTL was considered and rejected for that reason).
- ESCAPE STACK IS CAPTURE-PHASE AND DELIBERATELY SEPARATE FROM SHORTCUT TYPING-ISOLATION (Step 1.9): dismissLayers uses a capture-phase document keydown listener over a LIFO stack and dismisses ONLY the topmost layer. It is intentionally NOT routed through shortcutRegistry's typing-isolation, because Escape-to-close must fire even while the user is typing in a modal's field (dismiss is not a "shortcut"). Global shortcuts (shortcutRegistry, bubble phase) DO honor typing isolation. A later step must not "unify" these two paths — the asymmetry is the point.
- SHORTCUT COMBO SEMANTICS (Step 1.9): "mod" = ctrlKey||metaKey so one combo string works on Windows/Linux/macOS; a bare-key shortcut ("?", "n") rejects events carrying ctrl/meta so it never collides with a modified combo. Typing isolation defaults ON (shortcut skipped while in a field); a shortcut meant to act while typing (e.g. mod+enter to send) must opt in with allowInTextField.
- DESTRUCTIVE SAFETY = TWO-STEP CONFIRMABLE (Step 1.9): createConfirmable/useConfirmable arm-then-confirm with a 4s auto-disarm — first trigger only arms, second within the window runs it — so a single stray keypress can never fire a destructive action (CANON ADHD "Only confirm destructive actions"; step req 4). Later destructive controls (Close Session discard, Replace Answer) must route through this rather than binding a bare action to a key/click.
- GLOBAL FOCUS RING VIA :where() + LAZY LISTENERS (Step 1.9): src/styles/keyboard.css adds one specificity-0 :focus-visible cyan ring for all native focusables, complementing primitives.css's per-class rings so non-primitive focusables get one free; imported in main.tsx after shell.css. dismissLayers and shortcutRegistry each attach their single document listener lazily on first registration and detach on last unregister, so the framework is fully inert with zero consumers — no bootstrap wiring in main.tsx beyond the CSS import.
- STEP 2.1 IS DESIGN/SPEC ONLY (Step 2.1): produced TRANSLATION-SPEC.md + the schema/prompt in code (src/services/translation/), NOT a runtime service or live model call — the service is Step 2.2, gates are 2.3. New `src/services/translation/` folder groups schema.ts/prompt.ts/index.ts under services/ (CONVENTIONS scopes services/ to non-UI logic incl. the engines; consistent with persistence living there); 2.2 adds the service here.
- CANONICAL PROMPT + SCHEMA LIVE IN CODE; SPEC QUOTES THEM, CODE WINS ON DRIFT (Step 2.1): TRANSLATION_SYSTEM_PROMPT (prompt.ts) and TranslationResult/parseTranslationOutput (schema.ts) are the single source of truth; TRANSLATION-SPEC.md documents and reproduces them but explicitly declares the code authoritative if they diverge. Chose one authoritative in-code copy over spec-as-authority so Step 2.2 imports the prompt rather than re-transcribing it (drift prevention). A later step editing the prompt must edit prompt.ts and re-sync the spec's quoted block.
- SCHEMA ADDS `reasoning` BEYOND THE REQUIRED THREE FIELDS (Step 2.1): the step names translatedPrompt/confidence/detectedGaps; added a required `reasoning` string per CONVENTIONS rule 6 (design the data shape for the Transparency card now) and CANON ADHD Feedback (must be non-judgmental). Step 8.2's transparency card consumes it. A superset of the required schema, not a contradiction — later steps may read reasoning but must keep the three core fields.
- VALIDATOR IS TOLERANT-BUT-STRICT (Step 2.1): parseTranslationOutput clamps confidence to 0-100 and rounds, treats missing/garbage confidence as 0 (safe — never fabricates a high score that would skip the gates), drops unknown gap ids + dedupes, fills a reasoning fallback — but THROWS TranslationSchemaError when translatedPrompt is missing/empty or the reply isn't an object. Only the buried-request field is load-bearing; LLM output is otherwise treated as noisy. Extracting JSON from raw response text (fences/streaming) is deliberately left to the 2.2 runtime, not the schema.
- GAP TYPE IDS ARE THE STABLE CONTRACT (Step 2.1): the six kebab ids in GAP_TYPES are shared by the model output, the schema, the detected-gaps UI, and the transparency card; renaming any requires updating prompt.ts + TRANSLATION-SPEC.md together. GAP_TYPE_LABELS supplies display strings so UI steps don't hardcode gap names.
- INJECTED MODEL-CLIENT SEAM, NOT A CONCRETE CLIENT (Step 2.2): translate() takes a TranslationModelClient (client.ts) as an injected dependency because the real proxy is Step 1.10 (absent). Step 1.10's proxy client implements the seam later with zero change to translate(); tests and the harness pass stubs. The seam is a single NON-streamed completion returning Promise<string> — translation emits one JSON object, not a user-visible stream (streaming is answer-display, PIPELINE Stage 5). TRANSLATION_MODEL="claude-sonnet-5" lives in client.ts provisionally; point it at 1.10's model registry once that exists.
- translate() NEVER THROWS — TYPED TranslationOutcome (Step 2.2): the step's "handle empty, huge, API failure without crashing" is met with a discriminated union {ok|empty|too-large|error} the caller switches on, chosen over throwing so the clarify UI (2.3) and composer (5.0+) render each case deliberately. Empty/too-large short-circuit before ever calling the client.
- HUGE INPUT REJECTED, NOT TRUNCATED (Step 2.2): input over MAX_TRANSLATION_INPUT_CHARS (24k chars ≈ ~6k tokens; provisional, per-call configurable) returns {too-large} rather than truncating — truncation could drop the real request, which in ADHD input often trails after preamble. CANON's 10/50MB limits are for loaded CONTEXT, not this box; revisit the cap when Step 1.10's registry exposes real token budgets.
- RESPONSE-TEXT JSON EXTRACTION LIVES IN THE SERVICE (Step 2.2): extractJsonObject (translate.ts) strips ```json fences and, if needed, slices first-"{" to last-"}" before JSON.parse, tolerating models that wrap JSON in prose. Kept in the runtime service, NOT schema.ts (which by the 2.1 boundary validates already-parsed objects only). Any parse or schema failure becomes {error}, never a throw.
- MANUAL HARNESS IS OFFLINE-BY-DEFAULT (Step 2.2): runTranslationHarness defaults to stubTranslationClient (canned schema-shaped JSON) so it runs the full input→service→validate→outcome path without the absent proxy; pass the real 1.10 client for genuine translations. Exercised by harness.test.ts. Satisfies the step's "small manual test harness"; a live-model manual run is parked on 1.10.
- ROUTING.md "THE ONE CHANGE" APPLIED + STALE LABELS FIXED (Step 1.10): changed routing.js's Balanced api string claude-sonnet-4-6 → claude-sonnet-5 (the required change; the spot check flagged the 4-6 string). Also corrected two now-stale DISPLAY strings the change orphaned — the MODELS-table label "Sonnet 4.6" → "Sonnet 5" (same line; it drives every dynamic reasoning string via MODELS[choice].label) and one hardcoded "Sonnet 4.6" inside a downgrade note — plus the false comment claiming claude-sonnet-5 doesn't exist. NO scoring logic touched (scoreComplexity / classifyDimensions / route branches / thresholds / signal math all unchanged); the engine's own suite tests.js, run offline via a CommonJS copy, still passes clean (0 monotonicity violations, all three tiers exercised, confidence-coupling + plan-gating + overrides correct). If strict minimalism is preferred the two label-string edits can be reverted; the api string is the only behavior-relevant change.
- MODEL REGISTRY = src/services/modelRegistry.ts (Step 1.10 deliverable #2): the three CANON LOCKED DECISION 3 strings as a typed Record keyed by ModelId (aligned with stores/types.ts and routing.js's MODELS keys), each with tier + label + plan gating (free: haiku/sonnet, paid: opus). Extended thinking is ModelCallOptions.extendedThinking — a per-call FLAG, not a fourth registry entry (there are exactly 3). isModelId/getModel/MODEL_IDS are the shared accessors the proxy and future model pickers use.
- PROXY IS A PLATFORM-AGNOSTIC WEB-FETCH HANDLER (Step 1.10 deliverable #1): the handler LOGIC lives in src/services/proxyHandler.ts (typed, fetch injectable, unit-tested), forwarding to the Anthropic Messages API with the key in the x-api-key header and passing the upstream body — streamed or not — straight back. The deploy entry api/proxy.ts is a thin wrapper OUTSIDE src/ (not in tsconfig include, not in the client bundle — the deploy platform builds it) that reads ANTHROPIC_API_KEY from server env. The key never enters the browser bundle. Chosen Request→Response shape so the same logic runs on Vercel Edge / Cloudflare / Netlify Edge; host-specific wiring is Step 12.3.
- PROXY CLIENT complete() IS THE TRANSLATION SEAM (Step 1.10): src/services/proxyClient.ts createProxyClient().complete is structurally a TranslationModelClient, so translate() calls models through the proxy with NO adapter — proven by an end-to-end test (translate() → proxyClient.complete → mocked fetch). stream() parses Anthropic SSE (content_block_delta/text_delta) into text deltas for answer display (Step 5.1). This closes the Step 2.2 injected-seam loop. Also retyped translation/client.ts's TRANSLATION_MODEL as ModelId (value unchanged) so it's checked against the registry — resolves a Step 2.2 parked note.
- routing.js/tests.js KEPT AT REPO ROOT AS SOURCE-OF-RECORD (Step 1.10): committed at root (where supplied) with THE ONE CHANGE applied, but NOT yet imported by the app — ROUTING.md says Phase 3 wires the engine in. CONVENTIONS.md's src/services/ placement + a routing.d.ts is deferred to that wiring step. Note tests.js is CommonJS and can't run via `node` under this repo's "type":"module" (verified via a scratch .cjs copy this step) — also a Phase-3 cleanup.
- CONFIDENCE GATE LOGIC = src/services/translation/gate.ts (Step 2.3): evaluateConfidenceGate returns proceed (>=80) / moderate (>=60) / clarify (<60) — boundaries 80 and 60 inclusive; gateTranslation maps translate()'s TranslationOutcome to a UI-ready GatedTranslation (proceed|moderate|clarify|empty|too-large|error). This is the TRANSLATION-side gate (proceed vs clarify + the moderate note) only; the ROUTING-side coupling (escalate one step at 60-79, floor at 4 below 60) already lives in routing.js and is deliberately NOT duplicated (CONVENTIONS: don't rebuild).
- CLARIFYING QUESTION IS TEMPLATED IN CODE, NOT MODEL-GENERATED (Step 2.3): buildClarifyingQuestion reflects the engine's best-guess translatedPrompt back through a fixed neutral template ("It sounds like you might be asking: X — is that it, or would you like to add a little more?"), so the tone is guaranteed non-judgmental per CANON ADHD Feedback ("never corrective, never making the user feel stupid"). Chosen over asking the model to phrase it, which can't be guaranteed kind; a test asserts no blaming words appear.
- CONVERSATION AREA NOW HOSTS THE 2.3 UI, AS A DEMO (Step 2.3): replaced Step 1.5's center-column placeholder <p> with <ConversationArea/>. New feature folder src/components/translation/ (per CONVENTIONS components/<feature>/) + global src/styles/translation.css in main.tsx. No live pipeline exists yet (composer 5.0, orchestrator 5.2), so ConversationArea renders a DEMO of the three gate states through the REAL gateTranslation logic — replaced by the live flow in 5.0+ (precedent: Step 1.4 rendered primitives as demo content). ClarifyPrompt exposes onRefine as a callback seam the 5.2 orchestrator wires to re-run translation; it does not own re-submission.
- GATE COLORS ARE NEUTRAL, NEVER RED (Step 2.3): the confidence badge is green (--status-online) at 80+ and cyan (--state-cognitive) at 60-79; the low-confidence clarify state reads as "let's check," not an error. Red (--action-destructive) stays reserved for destructive actions (CANON ADHD Sensory rule). No new color tokens added.
- CORPUS SCORING IS PLUGGABLE; DEFAULT JUDGE IS OFFLINE + OBJECTIVE (Step 2.4): runCorpus takes a CorpusJudge. The default gapDetectionJudge passes iff translation succeeded AND the case's expected gap category was detected — objective and offline, so the harness runs and reports without a network. It is a PROXY for translation quality, not the real measure; makeSemanticJudge(client) grades translatedPrompt against "what was actually needed" via the model and is the real-accuracy path (needs the deployed proxy). Chose a pluggable judge over hardcoding so the real semantic judge drops in later without touching the runner.
- PER-CATEGORY, NOT AVERAGED (Step 2.4): runCorpus computes overall = total passed / 50 AND each category's own rate; meetsTarget = overall >= 90 AND every category >= 80 (checked per category, per PIPELINE.md "reported per category, not averaged"). A category can fail even when the overall average looks fine — a test asserts exactly that.
- CORPUS LOADED VIA VITE ?raw, NOT node fs (Step 2.4): corpus.test.ts imports the corpus markdown with `...31_0_translation_test_cases.md?raw` (typed by tsconfig.app's existing types:["vite/client"]) instead of node:fs — because test files under src/ are typechecked by the browser tsconfig, which has no node types. parseCorpus stays pure (string in), so it needs no file I/O. First ?raw import in the repo.
- CORPUS PARSED, NOT HAND-TRANSCRIBED (Step 2.4): parseCorpus reads the 50 cases straight from the supplied markdown by splitting on the "### N." case headers and reading each case's Gap category code, "what was actually needed," and verbatim ```text block. Only the 4 corpus categories (TP/EID/CBR/TPWC) are represented — a subset of the six-gap taxonomy — each case tagged with one primary gap; scope-ambiguity/unstated-assumptions don't appear as primary categories in this corpus.
- V2 CHOSEN OVER V1 FOR COMPLEXITY.md/FINDINGS.md, USER-CONFIRMED (Step 3.1): both files were missing from the repo; two non-identical prior-art variants existed outside it (V1 "Unknown Type", V2 "Free and Paid"). Per the Step-1.3 standing rule (prior-art folders are unverified until confirmed — flag and ask first), stopped and asked rather than picking either myself. Evidence for V2: its documented scoring weights (analysis verbs ×1.2/cap 4.8, synthesis ×0.8/cap 1.6, design nouns ×0.8/cap 1.6, constraints ×0.6/cap 1.8, data points ×0.4/cap 1.2, etc.) match routing.js's actual code exactly, field for field; V1 describes a visibly different, apparently-abandoned scoring scheme with no correspondence to the shipped engine. User confirmed V2; copied in as-is, unmodified.
- routing.js LIVES IN BOTH PLACES ON PURPOSE (Step 3.1): copied byte-identical into src/services/routing.js (the app's import point, per CONVENTIONS.md's services/ layout) while KEEPING the root copy — did not move/delete it. Reasons: (1) tests.js's `require("./routing.js")` assumes root-relative placement and must keep working unmodified, since "do not rebuild the scorer" extends to not breaking its own test harness; (2) future step prompts' REQUIRED FILES checks name "routing.js" and might expect it at root, same reasoning as not renaming tests.js (below). A vitest check (routingService.test.ts "embedding integrity") asserts the two copies stay byte-identical, operationalizing FINDINGS.md item 7's own verification principle ("the tested engine is provably the shipped engine") for this integration rather than just trusting a one-time copy.
- routing.d.ts + routingService.ts, ZERO LOGIC CHANGES (Step 3.1): routing.d.ts is ambient types only (RouteInput/RouteResult/Dimensions/ComplexitySignals/ModelKey), matching routing.js's actual accepted/returned shape field-for-field — verified by the routingService tests, not asserted from ROUTING.md's prose alone. routingService.ts's route() is a one-line pass-through to the vendor engine; FREE_MODEL_KEYS/ROUTING_MODELS are direct re-exports. `gaps`/`state` are typed as optional per routing.js's own header-comment contract but documented as NOT read by the current scorer body — forward-compatible typing, not new behavior.
- ROUTING.js's ModelKey (haiku/sonnet/opus) IS A DIFFERENT KEYING SCHEME THAN modelRegistry's ModelId (Step 3.1, reconciled): routing.js/routing.d.ts key by short engine names ("haiku"/"sonnet"/"opus" — routingService.ts's ModelKey, matching the ModelKey type already anticipated in modelRegistry.ts at Step 1.10); modelRegistry.ts keys by the full api string (ModelId). A cross-check test confirms every routing.js api string exists in MODEL_REGISTRY with a matching id, so the two independently-authored sources (vendor engine vs. this app's registry) can't silently drift apart. Callers translating between the two use ROUTING_MODELS[key].api ↔ MODEL_REGISTRY[apiString].
- NO UI WIRING IN STEP 3.1 (deliberate scope boundary): the routing service is now a typed, tested, importable module, but nothing in the app calls route() yet — same "service built, not yet consumed" pattern as Steps 1.10/2.1. Step 3.2 is explicitly tagged "[SONNET to wire UI]" in its own title, so decision-logic/override UI is that step's job, not 3.1's. The pipeline orchestrator that calls route() on a live translated prompt is Step 5.2.
- tests.js STILL CANNOT RUN VIA NATIVE `node tests.js` (Step 3.1, re-confirmed, not fixed): the repo's package.json `"type":"module"` makes node treat the CommonJS tests.js as ESM and throw on `require`. Verified instead via a scratch .cjs copy (same technique as Step 1.10) — output unchanged (0 monotonicity violations, all 3 tiers, override honored). Deliberately did NOT rename tests.js to .cjs to fix this, despite Step 1.10 flagging it as a "Phase-3 cleanup": a future step's REQUIRED FILES check may expect the literal name "tests.js," and renaming a vendor-supplied file its own docs (FINDINGS.md, ROUTING.md) refer to by name risks a worse problem than the running inconvenience. Remains a manual-verification-only file.
- MODEL DROPDOWN BINDS DIRECTLY TO THE STEP-1.7 SessionState.model FIELD (Step 3.2): `model: ModelSelection` (default "auto") and `setModel()` already existed, built ahead of time anticipating exactly this control — no new store field, no new persistence wiring (SESSION_PERSISTED_KEYS already includes "model" since Step 1.7/1.8). ModelDropdown is a pure binding; all state lives in the pre-existing store.
- DROPDOWN NEVER GATES OPUS ITSELF; THE ENGINE'S NOTE DOES (Step 3.2): the Model dropdown always offers all 4 options (Auto + 3 models) regardless of plan — it does not hide/disable Opus on the free plan. routing.js's own override logic already blocks a free-plan Opus override and returns a visible note instead ("requires the paid plan... kept automatic routing"); disabling the option in the UI would duplicate that gate in two places and risk them drifting apart, and would also violate CANON's "never a black box" rule by hiding the choice rather than explaining the outcome. ModelRoutingDemo surfaces the note live so this is provable, not asserted.
- OPUS DESCRIPTOR "smartest" IS SCREENSHOT-CONFIRMED; HAIKU/SONNET ARE INFERRED (Step 3.2): the V3 screenshot shows exactly "Opus 4.8 — smartest" selected in the Model dropdown — used verbatim. Haiku's "fastest" and Sonnet's "balanced" are NOT visible in the screenshot (only one option is shown, not the open dropdown list); inferred to match ROUTING.md's own tier names (Fast/Balanced/Deep) phrased as adjectives, same eyeball-with-a-flag pattern as Step 1.2's pill colors. A future pixel/interaction check should open the real dropdown against the source design to confirm.
- overrideFromSelection() IS DEFENSIVE, NEVER THROWS (Step 3.2): a persisted `model` value that isn't "auto" and isn't a recognized ModelId (e.g. a stale api string from a future/removed model, or corrupted IndexedDB state) maps to `null` (auto) rather than crashing route(). Consistent with the app's existing defensive patterns (persistence.ts try/catch, gate.ts confidence clamping) — a UI control's stored value should never be trusted enough to crash the routing call.
- ModelRoutingDemo REUSES THE EXISTING plan FLAG, ADDS NO NEW STATE (Step 3.2): the demo's "Paid plan" checkbox is bound to the Step 1.7 accountStore.plan/setPlan (already-existing, already-persisted) so BOTH spot-checked behaviors — override always wins on paid, free plan gates Opus with a note — are demonstrable live from the UI, without inventing a demo-only toggle. Toggling it here has the same real effect a future billing/plan step would have; Step 3.2 doesn't add billing, per ROUTING.md ("a routing layer flag, not billing infrastructure").
- SAMPLE PROMPT IS FIXED, NOT USER-EDITABLE (Step 3.2): ModelRoutingDemo routes one hardcoded mid-complexity prompt ("design a caching strategy...") rather than exposing a text box, since there is no composer yet (Step 5.0) and building one would be scope creep beyond "wire the Model dropdown." The fixed prompt was chosen to actually reach Opus at complexity when unconstrained, so the free-plan downgrade note has something to show.
- STEP 3.3 IS A TEST-ONLY VERIFY, NO PRODUCTION CODE (Step 3.3): the confidence coupling was already in routing.js and did not need building — confidenceCoupling.test.ts only proves the translation→routing SEAM works end to end at the confidence field. It asserts on route()'s complexity/effectiveComplexity/notes generically (base complexity read from the result, not pinned to a magic number) so it stays robust if the scorer's exact scores shift. The <60 band is documented as routing's SAFETY NET — the normal path is the Step 2.3 clarify gate catching <60 before it ever reaches route() (CONFIDENCE_COUPLING.md item 3), so both layers are intentional, not redundant.
- TECHNIQUE REGISTRY IS THE EXECUTABLE SOURCE OF TRUTH; TECHNIQUE-MATRIX.md DOCUMENTS IT (Step 4.1): registry.ts (conflicts/dependencies as data) is authoritative; TECHNIQUE-MATRIX.md is the human-readable explanation and declares the code wins on drift. Chose data-in-code over prose-as-authority so the Step 4.2 scorer consumes the matrix directly and registry.test.ts enforces the invariants (symmetric conflicts, no dependency on a conflict) — a later step can't add an incoherent conflict/dep without a red test.
- CONFLICT/DEPENDENCY MATRIX IS A DELIBERATE DESIGN CHOICE, NOT SPEC-DICTATED (Step 4.1): CANON/PIPELINE name the techniques and say "each has an effect, conflicts, and dependencies" but do NOT enumerate WHICH. Picked a minimal coherent set — conflicts only for genuine contradictions (verbosity opposites simplify⟷detailed, ask-vs-answer socratic⟷{detailed,step-by-step}, and simplify⟷chain-of-thought), dependencies only for genuine implications (verify→chain-of-thought since verifying needs reasoning to check; comparative→examples since comparisons need concrete instances). A later step (e.g. 4.2 tuning, or 11.x) may add/adjust pairs; the invariant tests + symmetric-conflict rule are the contract to preserve, not the exact pairs. Consequence logged for 4.2: verify pulls chain-of-thought, which conflicts with simplify, so verify+simplify can't co-stack — intended.
- auto-detect IS A META MODE, NOT A COMPOSABLE TECHNIQUE (Step 4.1): it sets isMeta:true, has an empty promptFragment, and is excluded from COMPOSABLE_TECHNIQUE_IDS (11) — the 4.2 scorer selects among the other 11 when auto-detect is the chosen mode, and 4.3 never injects an auto-detect fragment. Kept in the registry (and the TechniqueId union) because CANON lists it and the dropdown offers it as a selectable option.
- MAX_TECHNIQUE_STACK=4 AND DEFAULT_TECHNIQUE=socratic LIVE IN THE REGISTRY (Step 4.1): the stack cap and the default are exported constants in registry.ts (single source), so Step 4.2 (which the spot check requires to enforce ≤4) and the Step 4.5 selector read them rather than hardcoding. Matches CANON Feature 4 ("stacks at most 4", "Socratic (default)").
- AUTO-DETECT IS RULE-BASED/DETERMINISTIC, NOT MODEL-SCORED (Step 4.2): autoDetectTechniques scores by lexical signal regexes (+ optional routing complexity/domain hint boosts), same rationale as routing.js (FINDINGS.md) — offline-testable, same input→same output, fully auditable for the Transparency card. A model-scored variant is a possible future upgrade (parked). The signal regexes are a heuristic first cut; a later step (or 11.x) may tune them — the CONTRACT to preserve is the coherence invariant (≤4, conflict-free, deps satisfied), enforced on every test case, not the exact keyword lists.
- GREEDY STACK WITH DEPENDENCY CLOSURES, DEP-BEFORE-DEPENDENT (Step 4.2): selection walks techniques in score order; for each it computes the transitive dependency closure (deps first), rejects the whole closure if it would conflict with an already-picked technique or overflow the cap, else adds it. This guarantees every selected technique's dependencies are present, no conflicts, ≤4 — by construction, verified per-case. Consequence (as flagged at 4.1): verify pulls chain-of-thought which conflicts with simplify, so verify+simplify can never co-stack. Order is dependency-first (chain-of-thought before verify, examples before comparative) so it's composition-ready for Step 4.3's "techniques in order".
- SOCRATIC BASELINE = FALLBACK, NOT ALWAYS-PRESENT (Step 4.2): Socratic gets a small baseline score so a bare/ambiguous question yields [socratic] (the CANON default). But when a stronger, conflicting technique (step-by-step/detailed) scores higher, Socratic is correctly dropped from the stack — the baseline only makes it the default *when nothing better fires*, not a permanent member.
- MANUAL TECHNIQUE SELECTION DOES NOT AUTO-ADD DEPENDENCIES (Step 4.2): selectTechniques with a specific technique returns exactly [that technique]; dependencies are an AUTO-DETECT concern (CANON: "Auto-detect ... respects conflicts and dependencies"). If a user manually picks Verify, they get Verify alone, not Verify+Chain-of-Thought — their explicit choice is honored literally. Step 4.3 composition and 4.5's UI rely on this.
- COMPOSITION ORDER IS THE FIXED CONTRACT (Step 4.3): COMPOSITION_ORDER (base-template, role-prime, question, directness, techniques, output-format, confidence-requirement) is PIPELINE.md Stage 4 verbatim and must not be reordered by any later step — a test asserts the order in BOTH the sections[] array and the assembled prompt string. composeFinalPrompt always returns all 7 sections (for the Transparency card) but omits empty ones from the assembled string.
- DIRECTNESS ENCODINGS LIVE IN COMPOSITION (templates.ts), NOT 4.4 (Step 4.3): the per-level tone text (1 supportive / 2 balanced default / 3 blunt) is composition's, since composition owns the assembled prompt. Step 4.4 builds the selector UI + auto-recommend and feeds a DirectnessLevel into composeFinalPrompt — it must NOT redefine these strings. Deliberately settled here so 4.4 doesn't re-decide the wording.
- ROLE-PRIME IS HOISTED, NOT DUPLICATED (Step 4.3): Role-Prime is both a fixed composition SECTION and a technique. When the Role-Prime technique is in the stack, its fragment fills the role section and is removed from the techniques body (an explicit `role` input overrides both; otherwise a neutral DEFAULT_ROLE_PRIME fills the slot). Keeps the 7-slot structure clean; a later step adding role logic should populate the role section, not add a second role instruction among techniques.
- OUTPUT FORMAT DEFAULT ENCODES CANON'S ADHD COGNITIVE-LOAD RULES (Step 4.3): the default output-format section tells the model "no dense walls, ≤3 paragraphs, break into sections, lead with the point, scannable" — CANON ADHD hard rules injected at composition so every answer inherits them. Overridable via input.outputFormat. This is where Feature-wide ADHD output constraints are enforced in the prompt; Step 11.1's audit should check it's present.
- CONFIDENCE REQUIREMENT TIES TO TRANSLATION CONFIDENCE (Step 4.3): the confidence-requirement section always tells the model to flag ambiguity/uncertainty; when the translation confidence is <80 (the Step 2.3 moderate band that still proceeds), it additionally injects the numeric score and a "make sure you're answering what was actually asked" nudge. Uses the SAME 80 threshold as the gate, deliberately.
- DIRECTNESS SELECTOR BINDS TO THE STEP-1.7 SessionState.directness FIELD (Step 4.4): directness/setDirectness (default 2) already existed and are already persisted (SESSION_PERSISTED_KEYS) — DirectnessDropdown is a pure binding, no new store state, same pattern as the Model dropdown (3.2). The chosen level feeds composeFinalPrompt's directness param; the production feed is the Step 5.2 orchestrator, the demo proves it now.
- DIRECTNESS METADATA vs ENCODING SPLIT (Step 4.4): src/services/directness.ts owns the level METADATA (labels, default, the recommend function); the per-level prompt TEXT stays in composition/templates.ts DIRECTNESS_ENCODINGS (settled at Step 4.3). Deliberately did NOT duplicate the tone strings — the selector picks a level number, composition turns it into prompt text. One source for the wording.
- DIRECTNESS OPTIONS HAVE NO DESCRIPTOR SUFFIX (Step 4.4): unlike the Model dropdown ("Opus 4.8 — smartest"), the directness options are plain "Directness Level N" because that is exactly what the V3 screenshot's closed dropdown shows — no "— supportive" suffix visible. The tone descriptions live in DIRECTNESS_LEVELS.tone for tooltips/the 4.5 selector, not in the option label. (If the open dropdown in the real design shows descriptors, a future pixel check can add them — same eyeball caveat as the Model dropdown's Haiku/Sonnet descriptors.)
- recommendDirectnessFromEmotion IS ONLY Overwhelmed→1 (Step 4.4): PIPELINE.md maps only the Overwhelmed emotion to a directness level (Level 1); the other emotions map to TECHNIQUES, not directness, so recommend returns null for them (caller keeps the user's/default level). The function exists now as the seam Phase-6 state detection wires into; it is not yet called anywhere (no state detection UI). Auto-recommend must never silently override a manual pick — a later step wiring it should surface it as a suggestion, per CANON "never a black box" (parked note).
- SessionState.technique WIDENED TO techniques: TechniqueId[] (Step 4.5, a real store-schema change, not just "the control"): Step 4.5's own spec text explicitly requires manual selection to support stacking (a conflicting "pair," "the 4-technique stack limit" enforced on manual choice) — impossible with the singular TechniqueId field Step 1.7 built. Before changing it, grepped the whole repo for consumers: only its own declaration + 2 generic-by-design references in persistence.test.ts existed, so the rename/retype had zero other blast radius. `["auto-detect"]` (a one-element array holding the meta id) represents auto mode; any other array is the user's literal manual stack, fed straight into composeFinalPrompt's existing `techniques?: TechniqueId[]` input. Updated together in one step, per STORE-CONTRACT.md's own rule: types.ts, sessionStore.ts (default, setTechniques, SESSION_PERSISTED_KEYS), STORE-CONTRACT.md (dedicated explanatory note, not just a silent table edit), and persistence.test.ts.
- dependencyClosure/anyConflict EXPORTED FROM autoDetect.ts, LOGIC UNCHANGED (Step 4.5): these were private helpers inside the Step 4.2 scorer; widened their export surface only (no line of their behavior touched) so the new manual-selection module reuses the EXACT SAME dependency-walk and conflict-check the auto-detect scorer uses, rather than duplicating it. This is "reuse an existing pure export," not "touch the scorer" — the step's scope boundary ("do not touch 4.2... if it looks wrong, stop") is about not rebuilding/fixing the scoring logic, which is untouched; a widened export is the same category of low-risk change as Step 3.2 adding overrideFromSelection to routingService.ts.
- MANUAL SELECTION AUTO-INCLUDES DEPENDENCIES, VISIBLY — A DELIBERATE DEPARTURE FROM THE SINGLE-TECHNIQUE selectTechniques() BEHAVIOR (Step 4.5): Step 4.2 decided selectTechniques("verify", ...) returns exactly [verify], no auto-added deps, because that function's manual path was "the user's one explicit choice, honored literally." Step 4.5's NEW multi-select path (selectManualTechnique) is a DIFFERENT code path solving a different problem and deliberately auto-includes a technique's dependency closure when manually checked (e.g. checking Verify also checks Chain-of-Thought), always VISIBLE in the resulting array/UI summary — never hidden. Chose this over blocking-with-an-error (forcing the user to hunt down and check the dependency themselves first) because CONVENTIONS rule 6 ("no black boxes... expose what it decided and why") is satisfied by VISIBLE automation, and forcing extra clicks to discover a prerequisite adds cognitive load this very step is supposed to reduce. selectTechniques()'s single-manual-id behavior is UNCHANGED and still used wherever a single manual id needs dispatching; selectManualTechnique() is new, only used by the Step 4.5 popover.
- TECHNIQUE DROPDOWN'S CLOSED STATE IS A <button>, NOT A <select>, VERIFIED AGAINST THE ACTUAL SCREENSHOT (Step 4.5): re-viewed Divergence_AI_App_Screenshot_V3.png specifically for this step (per its own instruction not to assume from the Directness dropdown or memory) — confirmed the Technique control's closed state is visually IDENTICAL to Model/Directness (label above, single value "Socratic", cyan chevron, glass background) — matched exactly via the same .primitive-dropdown wrapper class + a new trigger button styled to the existing select's CSS declarations (widened selector list, not duplicated rules). The underlying element is a <button> opening a popover (not a real <select>) because the OPEN state must support genuine multi-select, which native <select> cannot do — the screenshot shows no open state for any of the three dropdowns, so nothing here contradicts the reference image; Step 1.4 explicitly anticipated this exact situation ("a feature step needing a richer listbox... builds on top of this rather than this primitive being replaced").
- TECHNIQUE POPOVER IS THE STEP-1.9 KEYBOARD FRAMEWORK'S FIRST REAL CONSUMER (Step 4.5): useDismissableLayer (Escape closes it, topmost-layer semantics) and useFocusTrap (Tab contained inside while open, focus returns to the trigger button on close) are used exactly as designed at Step 1.9, unmodified — resolving that step's own PARKED note ("no component consumes the keyboard framework yet"). Outside-click-to-close is a NEW small addition (a document mousedown listener scoped to when the popover is open) since the Step 1.9 framework covers Escape/focus/typing-isolation/destructive-confirm but not click-outside — logged here as the first outside-click pattern in the repo, available for later overlays (e.g. Step 9.x's revolving-door accordions) to copy.
- CHECKLIST VIEWPORT CAPPED AT ~5 VISIBLE ROWS (Step 4.5): CANON's "never more than 5-7 simultaneous choices in a view" is read literally — the 11-technique checklist has a fixed max-height with internal scroll so at most ~5 rows are visible without scrolling, rather than only relying on "Auto-detect is the default" to satisfy the rule. Both together (default hides the list entirely; the list itself is capped when opened) are the step's own suggested two-part answer ("Auto-detect being the default is most of the answer; the manual list is the part that needs a shape that doesn't dump all 12 at once").
- draftInput ADDED (NOT RENAMED) TO SessionState (Step 5.0): a genuinely new field, `draftInput: string` default `""` — the step's own text is explicit that the textarea's content must be session-store state, not local component state, specifically so the existing Step 1.8 autosave covers it ("a crash mid-thought must not cost the user the thought"). Straightforward per STORE-CONTRACT.md's ADD rule (unlike 4.5's rename, no existing-consumer check was needed since it's brand new). Cleared to "" on every TRANSLATE & ASK submit (not on a timer, not on blur).
- THE THREE STEP 3.2/4.4/4.5 DEMO CARDS ARE REMOVED, NOT LEFT ALONGSIDE THE REAL CONTROL ROW (Step 5.0): ModelRoutingDemo/DirectnessDemo/TechniqueSelectionDemo each mounted the SAME dropdown component (ModelDropdown/DirectnessDropdown/TechniqueDropdown) that ControlRow now also mounts, bound to the identical session-store fields — leaving both would put two live "Model" (etc.) controls on screen simultaneously, redundant and confusing, not helpfully redundant. Each demo's own PARKED note (written at 3.2/4.4/4.5) already flagged this exact supersession as expected. Deleted the three files entirely (verified no test referenced them first) rather than leaving dead code; the underlying SERVICE logic each demo proved (routingService.route, composeFinalPrompt directness/technique feeds) remains fully unit-tested regardless of the UI demo's existence. ConversationArea (Step 2.3) was deliberately left untouched — it's the conversation/history area, Step 5.1's scope, not this one's.
- COMPOSER.TSX IS THE REAL DELIVERABLE; ComposerSection IS THE TEMPORARY WRAPPER (Step 5.0): Composer (input box + control row, emits via onSubmit prop) has zero knowledge of any "last request" demo readout — it is exactly what Step 5.2's orchestrator will mount, unchanged, passing its own real onSubmit. ComposerSection wraps it with local useState + a JSON readout purely so this step's own "emits a complete typed object" claim is provable right now, before an orchestrator exists — same demo-wrapper pattern as every prior Phase 3/4 step, but this time the WRAPPED component (not just the demo) is the lasting deliverable.
- INPUT BOX AUTO-GROWS VIA JS scrollHeight, CAPPED AT 480px THEN INTERNAL-SCROLLS (Step 5.0): satisfies "it grows, it does not clip, it does not force a length" literally for realistic ADHD-rambling input (several paragraphs) while avoiding an unbounded-height box breaking page layout for a truly enormous paste — nothing is hidden/truncated, it's still fully there, just scrollable past the cap. No `maxLength` attribute anywhere; typing is never blocked. The "0 / 2000" counter (screenshot-matched exactly) is informational only — verified by test that huge input (50k chars) still passes through buildTranslateAskRequest completely unclipped.
- TRANSLATE & ASK HAS NO CLIENT-SIDE VALIDATION GATE (Step 5.0): the button is never `disabled` — pressable on a cold load with nothing typed and nothing configured (CANON ADHD Decisions rule: sensible defaults everywhere). Empty/huge input handling is translate()'s job (Step 2.2, already built and tested), not the composer's; the composer's only responsibility is emitting the raw text faithfully.
- HONEST SCREENSHOT DIFFERENCES, LOGGED PLAINLY PER THE STEP'S OWN INSTRUCTION (Step 5.0): (1) the State Detection panel the screenshot shows between the input box and the control row is NOT built — that's Step 6.3's scope, explicitly out of bounds here; the composer currently goes straight from input box to control row. (2) NO icons anywhere (brain icon beside "WHAT'S ON YOUR MIND?" and inside TRANSLATE & ASK, paperclip/context glyphs on Attach/Context) — continues the repo-wide icon debt from Step 1.5, not newly introduced here; Attach/Context use plain-unicode chevron characters ("▾"/"›") as placeholders. (3) On a cold load the three dropdowns read Auto/Level 2/Auto-detect (CANON defaults), not the screenshot's frozen mid-session values (Opus 4.8/Level 2/Socratic) — correct per CANON's defaults, not a bug, but a real visible difference from the static reference image at first paint.
- NO NEW GLOBAL SHORTCUT ADDED (Step 5.0): the step's ADHD-rules section mentions Step 1.9's typing-isolation framework only to say typing must not accidentally fire one; it does not ask for a submit shortcut (e.g. mod+enter), so none was added. The textarea is simply never a target the Step 1.9 shortcutRegistry would act on unless a future step explicitly registers one with allowInTextField.
- ConversationMessage GAINED confidence/downgraded/notes, NOT rating (Step 5.1): STORE-CONTRACT.md named "Steps 5.1-5.2" as the owner of this provisional type's full detail — confidence/downgraded/notes are added now because the FINISHED-answer display needs them persisted (so a reload still shows "N% confident..." and any router honesty note under a HISTORICAL answer, not just a freshly-streamed one — the screenshot shows the confidence line as a permanent part of the message, not a transient toast). `rating` was deliberately NOT added: Step 8.1 ("Feedback rating") owns that field's shape and its save/learning-loop behavior; adding it now would presume a design 8.1 hasn't made yet. The 5-star row (RatingRow) is still fully real/clickable today — it just doesn't persist anywhere until 8.1 wires it.
- streamAnswer() TAKES AN AsyncIterable<string>, MATCHING proxyClient.stream()'S SHAPE EXACTLY (Step 5.1): Step 1.10's createProxyClient().stream() already yields text deltas as an AsyncGenerator<string> — streamAnswer's first parameter accepts that shape directly, so Step 5.2's real orchestrator can pass the live proxy stream straight in with zero adapter code. The four named stages (translating/routing/composing/answering) are yielded by WHOEVER drives the pipeline (the demo, or Step 5.2), not by streamAnswer itself — streamAnswer only owns the token-arrival→done transition, since only the driver knows how long the real translate()/route()/composeFinalPrompt() calls actually take.
- STAGE DELAYS ARE A DEMO-ONLY CONCERN, NOT A RENDER-SPEED VIOLATION (Step 5.1): the offline demo sleeps 350ms per stage / 40ms per token purely so a human watching can perceive the cycle; CANON's "every visual interaction responds under 300ms" governs RENDER responsiveness (state arrives → DOM reflects it, which is instant here, plain synchronous JSX with no CSS transition to await), not how long a real pipeline stage takes to produce that state — that's exactly what the stage indicator is FOR (CANON Time rule: "show an indicator for any wait over 1 second"). Step 5.2's real driver will yield stage states as fast or slow as the actual translate/route/compose calls take; nothing here hardcodes a pipeline duration.
- THE STEP 2.3 GATE/CLARIFY DEMO CARDS WERE REMOVED FROM ConversationArea, THE UNDERLYING CODE WAS NOT (Step 5.1): gate.ts, TranslationCard.tsx, ClarifyPrompt.tsx, ConfidenceBadge.tsx are untouched and still fully unit-tested — only the THREE STATIC SAMPLE CARDS that demoed them inside ConversationArea are gone, replaced by real session.conversation rendering + the streaming demo. They become relevant again the moment Step 5.2's orchestrator decides (via gateTranslation) to show a clarifying question instead of streaming an answer — that wiring, not new UI, is what's still needed.
- ORCHESTRATOR = ASYNC GENERATOR OF TYPED PER-STAGE EVENTS, DEPS INJECTED (Step 5.2): runPipeline(request, {client, plan, signal}) yields one PipelineEvent per stage output (stage/translation/route/techniques/composed/streaming/done/error) — chosen over a callback bag or a single promise so each handoff is observable/loggable the moment it exists and terminal early-exits are structural (the generator returns after a non-proceed/moderate translation event; routing is provably never called on clarify). No store imports and no concrete network client in the service (same seam pattern as translate()); createProxyClient() satisfies the PipelineModelClient slice as-is. PIPELINE-CONTRACT.md documents every handoff; the code is declared authoritative on drift.
- TECHNIQUE SELECTION + COMPOSITION SHARE THE "composing" DISPLAY STAGE (Step 5.2): PIPELINE.md has five build stages but Step 5.1's PIPELINE_STAGES fixes four display names verbatim (translating/routing/composing/answering) — resolved by yielding stage "composing" once, before technique selection, covering both. Neither list was altered.
- STAGE-3 DISPATCH + HINTS (Step 5.2): stored techniques containing "auto-detect" (or empty/corrupted after filtering) → autoDetectTechniques on the TRANSLATED prompt with hints {complexity: route.complexity (base, NOT effectiveComplexity — confidence escalation is about model choice, not technique fit), domain: route.dimensions.domain}; any other array is the user's literal manual stack, order preserved, defensively filtered to known composable ids AND re-truncated to MAX_TECHNIQUE_STACK (the truncation added after a Step 5.2 subagent audit caught that the manual path trusted Step 4.5's UI for the ≤4 cap — CANON's numbers are hard rules, so the orchestrator enforces it too).
- done CARRIES routing.js's notes VERBATIM, THE GATE NOTE IS NOT DUPLICATED INTO THEM (Step 5.2): the finished answer's notes are exactly RouteResult.notes (the 60-79 escalation note among them is the permanent visible moderate-confidence marker); Step 2.3's MODERATE_CONFIDENCE_NOTE renders in the TranslationCard during the run instead. Chosen over prepending the gate note into done.notes, which would say the same thing twice in one AnswerMeta block. Nothing is filtered or dropped — a test asserts done.notes === route.notes.
- COMPOSED PROMPT SENT AS THE SINGLE USER MESSAGE TO THE ROUTED apiString (Step 5.2): execution calls client.stream({model: route.apiString, input: composed.prompt, extendedThinking: route.thinkingApplied || undefined}) with NO separate system prompt — composition's 7-section prompt IS the whole instruction set (a separate answering system prompt would be a second, competing instruction channel no spec asks for). Extended thinking is exactly what routing decided, never re-derived.
- CenterColumn REPLACES ComposerSection + StreamingAnswerDemo, BOTH DELETED (Step 5.2): new components/pipeline/CenterColumn mounts the UNCHANGED Composer and ConversationArea (now accepting children for the transient run UI) — the Step 5.0 JSON-readout wrapper and Step 5.1 offline demo (plus streamingDemo.ts) existed only because no orchestrator did, and both their own log entries predicted this supersession. Step 2.3's TranslationCard/ClarifyPrompt render live at last (every gated kind, including clarify). Refine appends the user's addition as its own user message and re-runs the WHOLE pipeline on original+"\n\n"+addition (translation needs the full thought; the addition alone usually isn't self-contained), with settings re-read fresh from the store.
- CONVERSATION APPENDS ARE THE HOST'S JOB, NOT THE SERVICE'S (Step 5.2): CenterColumn appends the raw input as a user message on non-empty submit only (whitespace-only submits still run — translate() owns empty-handling per Step 5.0 — but add no empty bubble), and appends the assistant message on done with confidence/downgraded/notes. A resubmit aborts the in-flight run via AbortController. The in-flight run itself is deliberately NOT persisted.
- MessageBubble/StreamingAnswer IMPORTED CROSS-FOLDER INTO components/translation/ConversationArea.tsx (Step 5.1): ConversationArea.tsx stayed in its Step 2.3 location (components/translation/) rather than moving to components/streaming/ or a new components/conversation/ — it's becoming a more general "conversation history" host than a translation-specific one, but relocating it wasn't necessary to complete this step and CONVENTIONS already has precedent for cross-feature-folder imports (Step 5.0's ControlRow imports from routing/directness/techniques). Flagged here rather than silently left; a future step is free to rename/relocate if the translation/ location keeps feeling wrong, but it isn't blocking anything now.
- RESILIENCE WRAPS deps.client INSIDE runPipeline, NOT AT THE CALLER (Step 5.3): makeResilientClient is applied once, at the top of runPipeline, over the raw PipelineModelClient every caller passes — rejected wrapping it in CenterColumn (or wherever createProxyClient() is instantiated) because that leaves resilience opt-in per call-site, exactly the kind of thing a future blind session could forget. Baking it into the orchestrator means every consumer (production UI, tests, any future harness) gets retry+timeout by construction. PipelineDeps.client's doc comment now explicitly says "do not pre-wrap."
- runPipeline NEVER THROWS — EVERY STAGE GETS ITS OWN try/catch (Step 5.3): extends translate()'s Step 2.2 never-throws guarantee to the whole generator. Routing/technique-selection/composition are pure and synchronous with no real throw path (confirmed by the Step 5.2 stage audits) — their boundaries are defensive-only (convert an unexpected bug into a neutral terminal event instead of crashing), NOT retried (retrying a deterministic function that just threw on the same input throws again). Only the two network-touching calls (translation's complete, execution's stream) get real retry/timeout via the resilient client.
- STREAM RETRY IS TIME-TO-FIRST-TOKEN ONLY (Step 5.3): makeResilientClient's stream() wrapper retries a failure ONLY if it happens before any chunk has been yielded. Once the model has started producing real output, a failure ends the run as a terminal error instead of retrying — retrying after partial output would replay from the start and show the user duplicated/reset text. This is the one place retry policy differs between complete() (whole-call retry, since translation is a single non-streamed JSON reply) and stream().
- NO NEW UI FOR RETRIES/TIMEOUTS (Step 5.3): CANON ADHD "no countdowns, no forced timeouts" governs what's user-visible, not whether an internal safeguard against a hung request may exist — the timeout IS real (default 30s per attempt, graceful — ends in the existing neutral error copy, never a ticking countdown). No new "retrying…" indicator was added: the existing per-stage indicator (Step 5.1/5.2, visible from the moment a stage starts, before any wait begins) already satisfies "show an indicator for any wait over 1 second" as a strict superset, and a flickering retry sub-state would itself risk CANON's "no competing visual stimuli" rule. Retries happen silently behind the same, already-visible stage indicator.
- RESILIENCE DEFAULTS: 3 ATTEMPTS, 300ms DOUBLING BACKOFF, 30s PER-ATTEMPT TIMEOUT (Step 5.3): picked as reasonable defaults for a proxy call with no documented SLA in CANON/PIPELINE/ROUTING; all three are overridable per-call via PipelineDeps.resilience (tests use tiny values so retry/timeout paths run in milliseconds, not real seconds). Revisit if the real deployed proxy (Step 12.3) shows these need tuning.
- STAGE 2-4 ERROR BOUNDARIES ARE UNTESTED-BY-FORCE, HONESTLY (Step 5.3): routing.js/technique-selection/composition have no real throw path reachable from valid pipeline input (the Step 5.2 stage audits already established this), so their new try/catch boundaries could not be exercised with a genuine forced failure without hacking protected/pure logic purely for test theater — declined to write a fake test that would trivially always pass. Only the two network stages (translation, execution) have real forced-failure tests (resilience.test.ts + 4 orchestrator integration tests). If a future step gives routing/techniques/composition a real fallible path (e.g. Phase 6 state detection touching one of them), that step should add the forcing test then.
- TELEMETRY IS A TEE AROUND runPipeline, NOT A CHANGE TO IT (Step 5.4): built observePipeline (services/telemetry/) as a wrapper that consumes runPipeline's own event stream and re-yields it unchanged, rather than adding logging calls inside orchestrator.ts itself. Chosen so the orchestrator (already fresh-context-audited at Step 5.2, error-boundary-hardened at Step 5.3) stays untouched — a third step editing the same file for a third unrelated concern was exactly the kind of repeated-touch risk CONVENTIONS' "one owning step per file" spirit warns against. Precedent: CenterColumn/usePipelineRun (Step 5.2) already established the "wrap and re-yield" tee pattern for consuming PipelineEvent; observePipeline is the same shape one layer earlier.
- TELEMETRY LOG IS IN-MEMORY, NOT A THIRD STORE, NOT YET PERSISTED (Step 5.4): CANON "STORES AND PERSISTENCE" plans exactly two Zustand stores and neither CANON nor STORE-CONTRACT.md names a telemetry field — adding one now would presume a persistence-duration decision (does it survive a reload? sync across sessions?) that isn't this step's to make. log.ts is a bounded (200-entry) in-memory ring buffer with a subscribe seam; whichever step builds the first real reader (Token Usage panel Step 9.6, or Learning Loop Step 10.1, which explicitly needs 15+ questions of history to survive at least the session) should decide then whether entries need IndexedDB persistence, and wire it FROM this module rather than duplicating the log.
- TOKEN USAGE CAPTURED VIA AN ADDITIVE onUsage CALLBACK ON ProxyCompletionRequest, NOT A RETURN-TYPE CHANGE (Step 5.4): complete()'s Promise<string> and stream()'s AsyncGenerator<string> are both UNCHANGED — changing either to return a richer object would ripple through the TranslationModelClient (Step 2.2) and PipelineModelClient (Step 5.2) seams, both established contracts other steps already build against. An optional onUsage(usage) callback lets any caller that wants token counts ask for them without disturbing callers that don't (translate.ts never sets it — its narrower ModelCompletionRequest type doesn't even have the field — and still works exactly as before). Parses Anthropic's real response shapes: complete() reads response.usage; stream() accumulates message_start's input_tokens and message_delta's output_tokens across the SSE event stream, reporting once at the end.
- translationTokens/executionTokens KEPT SEPARATE, NOT SUMMED (Step 5.4): the step's own wording is "token counts in and out per call" (plural calls per request — translation's completion and execution's stream are two separate model calls); summing them into one pair of numbers would lose which call cost what, information a future Token Usage panel or cost-analysis reader would want back. A consumer that only wants a total can add the two pairs itself; the reverse (splitting a stored sum back into components) isn't possible.
- DETECTION SERVICE MIRRORS THE TRANSLATION SERVICE, DELIBERATELY (Step 6.1): src/services/detection/ copies translation/'s 3-part shape (schema + client-seam + never-throws service) field-for-field rather than inventing a new pattern — because PIPELINE.md's RESOLVED note says detection fires ON THE SAME BUTTON PRESS ALONGSIDE translation on the SAME input, so keeping the two outcome unions/seams parallel makes the Step 6.3/6.5 code that drives both at once uniform. detectState()'s DetectionOutcome {ok|empty|too-large|error} is the exact shape of translate()'s TranslationOutcome; DetectionModelClient is structurally identical to TranslationModelClient (so one proxyClient.complete satisfies both, no adapter).
- DETECTION DIMENSIONS REUSE stores/types.ts UNIONS, NO NEW ENUMS (Step 6.1): StateDetectionResult's four values are EmotionState/RsdLevel/InterestLevel/CognitiveMode straight from stores/types.ts (Step 1.7 fixed them, and StatePills already holds exactly these). Detection defines only the runtime-check ARRAYS (EMOTION_STATES etc.) alongside; toStatePills() projects a result onto StatePills with zero remapping. A future step must not re-declare these unions in detection/ — the store owns them.
- DETECTION IS TOLERANT WITH NO LOAD-BEARING FIELD; UNKNOWN → null, NOT A FABRICATED NEUTRAL (Step 6.1): unlike translation (which THROWS when translatedPrompt is missing, because a wrong request is catastrophic), parseDetectionOutput throws ONLY on a non-object reply — every one of the four dimensions may independently be null. An unknown/garbage dimension value becomes null (pill simply not shown), NOT a defaulted middle value (calm/medium/processing), because a fabricated neutral pill would mislead the user about what was actually detected (CANON: pills are what the app really read, correctable, never a black box). A missing/garbage confidence → 0 (unsure), never fabricated high.
- DimensionReading CARRIES PER-DIMENSION confidence, NOT ONE OVERALL SCORE (Step 6.1): each of the four reads gets its own {value, confidence} because they are independent classifications — the transparency card (Feature 8) will want to show how sure each pill is, and a low-confidence dimension can be filtered/de-emphasized by the UI (6.3) without affecting the others. Rejected a single result-level confidence as lossy. The summary string (user-facing panel line) is separate from all four.
- SYSTEM IMPACTS ARE DERIVED IN CODE, NOT EMITTED BY THE MODEL (Step 6.2): STATE_IMPACTS (impacts.ts) maps each detected state VALUE → its directness/technique/tone impact; the model returns only the four values, and impactsFor()/recommendedDirectness()/suggestedTechniques() derive the impacts deterministically. Chosen over asking the model to also emit the impact so the mapping is auditable and can't be hallucinated into a different recommendation — same deterministic-where-possible philosophy as routing.js/technique auto-detect (FINDINGS.md). STATE_IMPACTS is the single authoritative source the prompt, the spec, and Step 6.5's feed all read.
- "X or Y" IMPACTS RECORD BOTH AS CANDIDATES; 6.5 RECONCILES, DOESN'T BLIND-STACK (Step 6.2): PIPELINE.md's "High interest → Detailed OR Comparative", "Analytical → CoT OR Step-by-step", "Creative → Metaphor OR Comparative" are stored as two-element techniques[] arrays labelled candidates. STATE_IMPACTS only records what each state points at; Step 6.5's state-feed must run these through auto-detect's existing conflict/dependency/≤4-cap logic (not stack all of them, and note e.g. analytical's CoT+Step-by-step don't conflict but socratic⟷step-by-step would if emotion=calm co-fires). Deferring the resolution to 6.5 keeps 6.2 to "what the state means," not "how techniques combine."
- recommendedDirectness() DUPLICATES-BY-DESIGN directness.ts's recommendDirectnessFromEmotion, KEPT CONSISTENT BY TEST (Step 6.2): both encode "Overwhelmed → Level 1, else null." Rather than have one call the other (cross-service coupling between detection/ and the directness service), they're independent one-liners with a test asserting they agree across all five emotions. If a future step changes the emotion→directness mapping it must change both (the test fails otherwise). Acceptable duplication for two trivial pure functions; revisit only if the mapping grows complex.
- DETECTION_SYSTEM_PROMPT REWRITTEN, NOT the SCHEMA (Step 6.2): 6.1's schema.ts was already final (tolerant, no load-bearing field); 6.2 confirmed and documented it rather than changing it. Only prompt.ts changed (the provisional 6.1 classifier → the haiku-tuned, impact-naming final). A test (impacts.test.ts) asserts prompt/table consistency so the two can't silently drift — same protection technique as routing.js↔MODELS and technique-registry↔TECHNIQUE-MATRIX.
- DETECTION FIRES ALONGSIDE THE PIPELINE, NOT INSIDE IT (Step 6.3): PIPELINE.md's RESOLVED note describes State Detection as its own single on-demand classification on the SAME input as translation, not one of the five stages orchestrator.ts runs — so CenterColumn calls detectState() as a parallel side effect inside startRun() (sharing the submission's AbortController), never touching orchestrator.ts (already fresh-context-audited at 5.2, error-boundary-hardened at 5.3 — a 3rd unrelated edit was exactly the repeated-touch risk avoided at telemetry, Step 5.4, for the same file). Consistent with Step 5.4's own "wrap and re-yield" precedent, but detection isn't even a tee on the pipeline's events — it's a fully independent call.
- DETECTION RESULT SPLITS INTO RICH LOCAL STATE + THE EXISTING statePills STORE FIELD (Step 6.3): CenterColumn keeps the full StateDetectionResult (confidence, summary — needed for display/correction) in local React state, NOT persisted, same treatment as the in-flight pipeline run itself (Step 5.2 PARKED: "the in-flight run itself is NOT persisted"); only the four projected VALUES go to session.statePills (Step 1.7's pre-existing field) via toStatePills(), which is the durable "one source" Step 6.5's state bus will read. Rejected inventing a third store or persisting the rich result — CANON/STORE-CONTRACT plan exactly two stores and neither names confidence/summary as persisted fields.
- REFINE RE-FIRES DETECTION FOR FREE, BY SHARING startRun (Step 6.3): detection-firing lives inside startRun() (not handleSubmit), so ClarifyPrompt's "Refine and try again" path — which already calls startRun with the combined original+addition text — automatically re-classifies state on the refined input too, with zero separate wiring. If a future step ever splits startRun's pipeline-start and detection-start apart, it must keep both call sites (submit AND refine) firing detection, not just submit.
- PILL CORRECTION IN 6.3 IS DISPLAY-ONLY; RECORDING IS 6.4's JOB (Step 6.3): handleCorrectState (CenterColumn) updates the panel's displayed value and session.statePills immediately (so a correction visibly takes effect) but writes NOTHING to the account store and touches no correction-count/threshold logic — CANON's "After 15+ corrections... detection adapts" is explicitly Step 6.4's scope ("Correction learning"), not this step's. A future 6.4 implementation should hook into this SAME handleCorrectState call site (or an equivalent onCorrect callback) to also record the correction, rather than building a second, parallel correction-capture path.
- "Adjust" OPENS ALL VISIBLE PILLS' CORRECTORS AT ONCE (Step 6.3, a documented interpretation): CANON/PIPELINE.md name the Adjust control's existence but not its behavior; the screenshot shows it as a single button next to the summary line, distinct from clicking an individual pill (which opens just that one's corrector). Chosen as a bulk-edit toggle (open all / close all) so it has real, distinct behavior rather than duplicating a single pill click. A future step with clearer source material may redefine it.
- PANEL-LEVEL × vs PER-PILL ×: BOTH BUILT, DIFFERENT SCOPES (Step 6.3): the screenshot shows one visible × at the STATE DETECTION header (dismisses the whole panel for this turn); PIPELINE.md Feature 5's own text says "Pills are dismissible (X)" per pill. Read as non-contradictory (the screenshot doesn't show a hover-only per-pill affordance, it just isn't visible in one static frame) rather than the text/image actually disagreeing — same CANON precedence rule as Step 1.5's logout control (only overrides the image when they truly conflict, not when the image is merely silent). Built both: StateDetectionPanel's header × hides the whole panel; each StatePill's own × dismisses just that pill.

## PARKED
- ~~Marble texture files: location/source to be clarified before Step 3.1 (Marble system build).~~ RESOLVED at Step 1.3 — three texture files provided, saved, and wired into marble.css; see DECISIONS.
- Translation corpus: available for Step 2.4 (Translation verification) if needed; not required for Steps 1–2.2.
- No headless browser exists in the build environment and browser downloads are blocked by the network allowlist, so Step 1.1 layout was verified by serving the app and asserting the grid CSS values over HTTP plus a clean production build — not by rendered-pixel screenshot. First step run in an environment with a browser should eyeball the frame once.
- Vite template's public/ shipped favicon.svg and icons.svg placeholders are still unreplaced. The top-bar logo now exists (Step 1.6, Logo.tsx) and a static favicon could be derived from its brain SVG, but that was out of Step 1.6's scope ("logo component placed in the top bar"). A later polish/deploy step (e.g. 12.3) should export a static favicon from the logo mark.
- Step 1.2's three eyeballed pill colors (RSD, Interest, Cognitive Mode — see DECISIONS) are waiting on a future session with real pixel-sampling tools to confirm against the screenshot; current values are a confident visual match, not a measured one.
- Inter is referenced by --font-family-base but the font file itself is not yet loaded/self-hosted anywhere in the repo; waiting on Step 3.4 (Typography and spacing application).
- The two grey texture files (grey-marble-1.png / grey-marble-2.png, and their rendered supertiles grey-marble-slab-1.jpg / grey-marble-slab-2.jpg) read as visually near-identical to the black marble file — same vein pattern, barely distinguishable tone, not genuinely distinct textures. Flagged by the person supplying the files and deliberately deferred as cosmetic. Should be replaced with visually distinct textures before Step 11.4 (Visual verification). Currently moot for rendering since the grey files have no active consumer (see DECISIONS), but will matter once/if a future step gives grey marble a defined use.
- Step 1.3's slab implementation was verified by production build, HTTP serving checks, and a fresh-context subagent's static audit against the written rule — not by an actual browser resize test or pixel comparison (same build-environment constraint as Steps 1.1/1.2: no headless browser, network allowlist blocks browser downloads). A future step run in an environment with a real browser should do the literal 300px-resize vein-consistency check MARBLE-CONTRACT.md's enforceable checks call for, and confirm the Blue Marble button's background-attachment:fixed technique actually renders as continuous with the slab (the CSS is logically sound per the subagent audit, but its cross-browser rendering behavior — the exact thing the prior contract warned about for iOS Safari — is unverified here).
- ~~AppShell.tsx's current cards/button are Step 1.3/1.4 demo content proving marble continuity and exercising the primitive library, not real feature UI. Later steps (1.5+ for structure, and each feature's own step) replace this content; they don't need to preserve its exact markup or inline styles.~~ RESOLVED at Step 1.5 — demo content fully replaced by real TopBar/LeftNav plus center/right placeholders; see DECISIONS.
- Step 1.4's primitives were verified by production build (tsc + vite build, clean), dev server HTTP checks, and confirming the AppShell module transforms through Vite without import/syntax errors — not by an actual rendered/visual check (same build-environment constraint as every prior step: no headless browser). A future step in an environment with a real browser should confirm the focus rings are actually visible, the Dropdown's chevron and Pill borders render as intended, and Blue Marble button styling still reads correctly now that it's wrapped in a component rather than a raw button.
- Dropdown is a native `<select>`; if a later feature step (e.g. 4.4/4.5 Directness/Technique selector UI) needs custom rendering per option (icons, descriptions, multi-column), that's new work on top of this primitive, not a modification expected from Step 1.4.
- No icon library exists yet (Step 1.5): the logo, Settings/Notifications/Help buttons, and all left-nav items are text placeholders. Waiting on a future step to pick an icon approach (SVG set, icon font, or hand-built per CANON's logo spec for the one that needs custom animation) and wire it into TopBar.tsx/LeftNav.tsx.
- Center column (one placeholder `<p>`) and right column (two placeholder `GlassPanel`s) are waiting on their owning steps: Steps 5.0+ (input composer, streaming display) and 6.x (state pills, transparency, multi-AI) for center; Steps 9.4 (Visibility toggle), 9.5 (Revolving-door accordions), 9.6 (Quick Tools grid) for right. Not blocking anything now — placeholders are intentional at this stage.
- Step 1.5's shell was verified by production build, dev server HTTP checks, and confirming TopBar.tsx/LeftNav.tsx/AppShell.tsx transform through Vite without errors — not by an actual rendered/visual comparison against the V3 screenshot (same build-environment constraint as every step so far: no headless browser). A future step with a real browser should confirm topbar/left-nav spacing actually reads as the screenshot intends, particularly the topbar's three-group flex distribution and the left-nav's bottom-anchored Trash/System Status/Logout block.
- Step 1.6's logo was verified by production build and dev-server HTTP checks (Logo module serves, aurora tokens + reduced-motion guard + background-clip present) — not by an actual rendered check of the brain shape, gradient, or animation (no headless browser). A future step with a real browser should confirm the hand-built brain path actually reads as a brain at 32px, the aurora gradient renders on both the SVG stroke and the "AI" text (background-clip:text has cross-browser quirks), and the synapse pulse looks subtle rather than distracting. The brain path is hand-drawn and approximate — Step 11.4 (Visual verification) may want it refined against a designer's eye.
- The two stores are built and unit-verified but NOT yet consumed by any component — no UI reads or writes them. Feature steps wire them into the real controls (model/directness/technique dropdowns in 4.4/4.5/5.0, conversation in 5.x, state pills in 6.3, context in 7.x, ratings/prompts/visibility in 8.1/9.x). Not blocking anything; the stores stand alone correctly.
- ~~idb is NOT installed and nothing persists to IndexedDB yet — Step 1.8 does this.~~ RESOLVED at Step 1.8 — idb installed, persistence.ts written against the 1.7 seam, wired into main.tsx, kill-and-reload test added.
- Step 1.7 was verified by a temporary CommonJS-compiled runtime smoke test (26 assertions, all pass) plus clean production build/typecheck. Step 1.8 added a real test runner (vitest); the stores now also have indirect test coverage through persistence.test.ts. A dedicated store unit-test file is still Step 12.1's job.
- Step 1.8's raw 5-second setInterval firing is NOT directly unit-tested: vitest fake timers deadlock against fake-indexeddb's async scheduler, so the interval test was replaced with a pagehide-flush + stop()-cleanup test (real logic) plus direct saveNow()/loadPersistedState() tests. The interval itself is a one-line wrapper over the tested saveNow(). A future step could cover it with a real-timer integration test or a different IDB fake if desired.
- Step 1.8 restore-before-render blocks first paint on the IndexedDB read (milliseconds). No UI consumes the stores yet so it's invisible now; if a later step finds it costs perceptible startup time, switch main.tsx to render-then-hydrate with a "hydrating" flag rather than awaiting load before render.
- Autosave/restore is verified in a happy-dom + fake-indexeddb test environment, not a real browser. A future step with a real browser should confirm the actual 5s writes, real pagehide/visibilitychange flush, and true cross-reload restore behave as the tests assert (same no-headless-browser constraint as every prior step).
- Step 1.9's keyboard framework is verified by 14 happy-dom unit tests (all four required capabilities) + clean tsc/vite build + clean oxlint, NOT a real browser (same no-headless-browser constraint as every prior step; a dev-server boot check was attempted but blocked by the sandbox). A future browser-equipped step should confirm the focus rings are actually drawn, focus trap/return survives real-browser focus quirks (esp. that release() returns focus to the trigger), Escape dismisses the topmost overlay, and shortcuts feel right — and specifically re-check focusTrap under a real renderer, since happy-dom's focus()/activeElement is an approximation.
- No component consumes the keyboard framework yet — there are no modals or global shortcuts in the UI (still layout shell + inert nav + placeholders). Feature steps plug in: modals via useFocusTrap + useDismissableLayer, send/command shortcuts via useGlobalShortcut, destructive controls (Close Session discard, Replace Answer) via useConfirmable. Step 11.3 (Keyboard audit) will expect these already wired, not retrofit them (CONVENTIONS.md rule 5).
- ~~STEP 1.10 IS BLOCKED: REQUIRED FILE routing.js does not exist in the repo.~~ RESOLVED at Step 1.10 — routing.js + tests.js supplied; THE ONE CHANGE applied; proxy + registry + client built and stub-verified. Residual (below): the live deploy + real streamed call are still unrun in this no-network sandbox.
- ~~STEP 2.3 WILL ALSO BE BLOCKED: REQUIRED FILE CONFIDENCE_COUPLING.md does not exist.~~ RESOLVED — CONFIDENCE_COUPLING.md supplied; Step 2.3 is unblocked.
- STEP 1.10 DELIVERABLE #3 (deploy + live streamed call) IS UNRUN: the proxy (proxyHandler/api/proxy.ts), registry, and client are built and stub-verified, but NO live streamed Anthropic call has been made — the sandbox has no network and no ANTHROPIC_API_KEY, and the proxy is not deployed. When deploying (Step 12.3): set ANTHROPIC_API_KEY in the host env, adapt api/proxy.ts to the host runtime, point createProxyClient at the deployed endpoint, and confirm a real streamed call end-to-end.
- ~~Step 2.1's schema/prompt are present and unit-tested but NOT imported by any app entry yet, so they're tree-shaken out of the app bundle (build shows the JS bundle unchanged). Step 2.2's service is the first consumer; wiring into the actual composer UI is Steps 5.0+.~~ RESOLVED at Step 5.2 — CenterColumn → runPipeline → translate() puts the translation service, prompt, and schema in the shipped bundle.
- ~~STEP 2.2 DEPENDS ON THE 1.10 PROXY, WHICH IS ABSENT.~~ RESOLVED at Step 1.10 — createProxyClient().complete (proxyClient.ts) implements the TranslationModelClient seam; an end-to-end test drives translate() through it. A live network call still can't run in this sandbox (see the deploy residual above).
- ~~The 50-case translation corpus (31_0_translation_test_cases.md) is supplied, ready for Step 2.4's harness.~~ RESOLVED at Step 2.4 — parsed by corpus.ts, run by corpusHarness.ts, committed.
- STEP 2.4 REAL ACCURACY IS UNMEASURED: the harness runs all 50 cases and reports per category, but the committed run is a FORMAT sample from the offline stub client + gap-detection judge (TRANSLATION-CORPUS-REPORT.sample.md), NOT the engine's real accuracy. Measuring against PIPELINE's 90%/80% target needs the deployed proxy (Step 1.10 residual) driving translate() with a live client and makeSemanticJudge grading translatedPrompt vs "what was actually needed." Until then, whether the engine actually hits 90%/no-cat-below-80% is unknown.
- ~~routing.js is now importable but NOT consumed anywhere in the app.~~ PARTIALLY RESOLVED at Step 3.2 — ModelRoutingDemo now calls route() live from the UI. Still not wired into a REAL pipeline: the demo uses a fixed sample prompt, not a translated one, since the live translate→route flow is Step 5.2's orchestrator. Step 4.x (technique selection) is the next consumer of routing's domain/complexity output. FULLY RESOLVED at Step 5.2 — runPipeline now calls route() on every live translated prompt, and technique auto-detect consumes its complexity/domain output.
- STEP 3.2's UI COMPONENTS ARE TYPECHECK+BUILD VERIFIED ONLY, NOT RENDER-TESTED: ModelDropdown/ModelRoutingDemo have no React Testing Library coverage (same repo-wide choice as Step 2.3's translation UI) — the pure data (option list/labels) is unit-tested, but no test confirms the dropdown actually re-renders the routing result on change in a real DOM. A future browser-equipped step should click through the dropdown and the Paid-plan checkbox and confirm the result/notes update as expected.
- tests.js remains unrunnable via native `node tests.js` under this repo's "type":"module" (verify via a scratch .cjs copy, as done at Steps 1.10 and 3.1). Deliberately not renamed — see DECISIONS. If a future step wants native `npm run` execution of the vendor suite, the fix is a separate script/config (e.g. a package.json script that pipes tests.js through a CJS-aware runner), not renaming the file.
- V1 COMPLEXITY.md/FINDINGS.md variant (the "Unknown Type" folder) was reviewed but NOT copied into the repo — it describes an earlier, structurally different scoring model with no correspondence to the shipped routing.js. Left alone at its original external location; no repo reference to it beyond this note.
- Step 2.2's translation service is verified with STUB clients only (11 tests). NO live claude-sonnet-5 call has been made — the sandbox has no network. The wiring now exists (proxyClient.complete → api/proxy.ts); a genuine translation run happens once the proxy is deployed (Step 1.10 deploy residual above).
- Step 2.3's gate LOGIC is unit-tested (8 tests: 80/60 boundaries, full outcome→gated mapping, neutral clarifying-question tone), but the UI COMPONENTS (ConversationArea/TranslationCard/ClarifyPrompt/ConfidenceBadge) are only typecheck+build verified — no React Testing Library, no browser render. A future browser step should confirm the three gate cards read correctly, the clarify textarea + "Refine and try again" work by keyboard, and the badge colors match intent (green/cyan, never red). Same no-browser constraint as every prior step.
- ~~The center column now shows Step 2.3's gate UI as a DEMO (sample translations), not live output; the composer (5.0) and orchestrator (5.2) replace it with the real translate()→gateTranslation→onRefine flow.~~ RESOLVED at Step 5.2 — the real flow now runs end to end (offline-verified); the RIGHT column is still placeholder, owned by Steps 9.4/9.5/9.6.
- ~~The center column now stacks THREE demo cards (2.3 gate states, 3.2 ModelRoutingDemo, 4.4 DirectnessDemo), each proving a control/engine wired live on sample data. Step 5.0 (input composer + control row) replaces all of them with the real Model/Directness/Technique control row over one live conversation; Step 5.2's orchestrator drives the real translate→route→select→compose flow. These demos are scaffolding, not the final layout — they don't need to be preserved.~~ RESOLVED across Steps 5.0-5.2 — all demo scaffolding is gone; the center column is the live product.
- STEP 4.4's auto-recommend (recommendDirectnessFromEmotion) is defined + unit-tested but NOT called anywhere yet — no state detection exists (Phase 6). Phase-6 state detection should call it and surface Level 1 as a SUGGESTION for an Overwhelmed reading (never silently overriding a manual directness pick), per CANON "never a black box".
- STEP 4.4's UI components (DirectnessDropdown/DirectnessDemo) are typecheck+build verified only, not render-tested (no React Testing Library); the pure data (options, recommend, level→TONE) IS unit-tested. A future browser step should confirm the dropdown changes the composed TONE line on selection. Same caveat as the 3.2/2.3 UI.
- STEP 4.5's UI components (TechniqueDropdown/TechniqueSelectionDemo) are typecheck+build verified only, not render-tested — same repo-wide no-RTL caveat as every other dropdown. The RULE logic (manualSelection.ts) IS fully unit-tested, including both of the step's own explicit VERIFY criteria. A future browser step should specifically confirm: the popover opens/closes on click and Escape, Tab is trapped inside it while open, focus returns to the trigger button on close, outside-click closes it, and a disabled (conflicting/capped) checkbox row is visually distinguishable with its reason readable (currently only a `title` attribute — a real tooltip/inline text may read better, unverified without a browser).
- ~~The center column now stacks FOUR demo cards (2.3 gate, 3.2 routing, 4.4 directness, 4.5 technique), each proving a control/engine wired live on sample data — all scaffolding Step 5.0 (input composer + control row) replaces with one real control row over one live conversation.~~ RESOLVED across Steps 5.0-5.2 — see the line above.
- STEP 4.5's popover positioning (`position:absolute; top:100%`) is unverified in a real browser — no collision/overflow handling if the control row sits near a viewport edge (unlikely at 300px right-column width, but unconfirmed). A future browser step should check it doesn't clip or overflow the app shell.
- STEP 5.0's State Detection panel gap: CANON's layout places it between the input box and the control row; it is genuinely absent right now (not placeholder-stubbed) since it's explicitly Step 6.3's scope. Step 6.3 should insert its panel into Composer.tsx between <InputBox/> and <ControlRow/> — that's the one file/position a future session needs to touch, not a new layout decision.
- STEP 5.2's "REAL QUESTION THROUGH THE REAL DEPLOYED PROXY" VERIFY IS UNRUN: no network, no ANTHROPIC_API_KEY, no deployed proxy in this sandbox (the standing Step 1.10/12.3 residual). Everything short of the network hop is proven offline — 10 orchestrator tests with ONLY the model client stubbed (routing.js/auto-detect/composition run real), including both required VERIFY cases: a confident question flowing all five stages to a streamed done, and a vague <60 question stopping at the clarifying question with routing provably never called. The first deployed session must repeat both cases LIVE and record the real confidence/model/techniques observed.
- routing.js's OWN header comment (its lines ~3-6) lists a stale, shorter "Contract out" than what route() actually returns (missing modelLabel/tier/effectiveComplexity/rawPoints) — found by a Step 5.2 subagent audit. Left unfixed per "do not improve routing.js"; routing.d.ts + PIPELINE-CONTRACT.md document the true shape. A Phase-11 cleanup could fix the comment if vendor-file edits are ever sanctioned.
- STEP 5.2's UI components (CenterColumn/usePipelineRun, and ConversationArea's new children prop) are typecheck+build verified only — same repo-wide no-RTL choice as every prior UI step. A browser-equipped step should click through: submit → stage indicator → streaming text → finished message with confidence/notes, the clarify → refine loop, and a rapid double-submit (AbortController path).
- A reload mid-run keeps the submitted user message (session store) but drops the transient run UI — a partial answer or an un-answered clarifying question disappears. Acceptable for now (the draft-input crash protection covers the typed thought); revisit only if real usage shows an in-flight-run persistence need.
- STEP 5.0's UI (InputBox/ControlRow/AttachContextControls/TranslateAskButton/Composer) is typecheck+build verified only, not render-tested — same repo-wide no-RTL caveat as every prior dropdown/control. The step's own explicit VERIFY items ("type several paragraphs, confirm nothing clips/truncates/lags," "the button is pressable on a cold load") are satisfied by code inspection (no maxLength, no disabled state, JS auto-grow logic) but NOT exercised in a real browser — the "reload mid-sentence" item specifically WAS made concrete via the persistence kill-and-reload test extension, which is the one VERIFY item this sandbox's existing test infrastructure could actually exercise precisely.
- Icons remain entirely unbuilt (brain glyph beside "WHAT'S ON YOUR MIND?" and inside TRANSLATE & ASK; Attach/Context glyphs are plain-unicode placeholders "▾"/"›") — continuing the Step 1.5 icon-library debt. Still waiting on a future step to pick an icon approach and apply it repo-wide (TopBar, LeftNav, Composer, and anywhere else text stood in for an icon).
- Attach/Context's onAttach/onContext default to a no-op — Phase 7 (7.1 upload, 7.3 URL, 7.4 variables, 7.5 Context Snapshot panel) is what gives them real behavior; ComposerSection currently passes neither prop through (both fire the harmless default no-op). A future step should decide whether Attach opens a menu (file/paste/URL) or a single upload dialog, and whether Context opens a drawer/modal — the screenshot's "▾" vs "›" glyphs hint at that distinction (menu vs. navigate-to-panel) but neither behavior is implemented yet.
- STEP 5.1's UI (MessageBubble/AnswerMeta/RatingRow/StageIndicator/StreamingAnswer) is typecheck+build verified only, not render-tested — same repo-wide no-RTL caveat as every prior control. The pure state-transition logic (answerDisplay.ts's streamAnswer) IS unit-tested. A future browser step should confirm: the textarea/star buttons are keyboard-operable, the stage indicator visibly updates each step, streaming text visibly grows without layout jank, and the "Replay" button's key-remount actually restarts the demo cleanly (no stale timers firing into an unmounted DemoRun — the useAnswerDisplay effect's cleanup sets `cancelled=true`, which should prevent this, but it's unverified in a real browser).
- RatingRow's rating is NOT PERSISTED anywhere (Step 5.1, by design — see DECISIONS): clicking a star updates only that component's own local useState; nothing writes back to the session store or account store. Step 8.1 must decide where a rating lives (likely a new field on the relevant ConversationMessage, or a separate Rating record in the account store per STORE-CONTRACT.md's `ratings: Rating[]`) and wire RatingRow's onRate to it.
- The download button (RatingRow) has no real behavior — onDownload defaults to a no-op (the demo just console.info()s). Step 8.5/Feature 10's download modal (pick content + format, then download or copy) is what gives it a real action; this step only needed a focusable, clickable control matching the screenshot.
- ~~StreamingAnswerDemo's timing constants (350ms/stage, 40ms/token) are arbitrary demo pacing, not derived from any real latency measurement — Step 5.2's real driver determines actual timing from genuine translate()/route()/composeFinalPrompt()/proxy-stream calls, which will look nothing like this fixed cadence.~~ MOOT as of Step 5.2 — StreamingAnswerDemo.tsx was deleted (superseded by the real CenterColumn/observePipeline driver); Step 5.4's telemetry now records the REAL per-stage timings (stageDurationsMs), still unmeasured against a live proxy per the standing Step 1.10/12.3 residual.
- MAX_TRANSLATION_INPUT_CHARS (24k) is a provisional guess — no CANON/PIPELINE number exists for the translation input box. Revisit against real per-model token budgets once Step 1.10's model registry exists.
- STEP 5.4's telemetry log has no reader yet (Token Usage panel: Step 9.6; Learning Loop: Step 10.1; Transparency card: Step 8.2) — getTelemetryEntries()/subscribeTelemetry() are exercised only by observePipeline.test.ts, not by any UI. Not blocking anything; the log accumulates correctly regardless of whether anything reads it yet, same "service built, not yet consumed" pattern as Steps 1.10/2.1/3.1.
- STEP 5.4's real token counts are UNMEASURED against a live proxy (same standing residual as every step since 1.10): the onUsage parsing (proxyClient.ts) is verified against hand-built SSE/JSON fixtures matching Anthropic's documented response shape, not a real API response. The first deployed session should confirm a live call's usage numbers actually populate translationTokens/executionTokens as expected.
- STEP 5.4's timings measure wall-clock stage duration in THIS sandbox (synchronous routing/techniques/composition are near-instant; the network stages are stubbed), not real latency — meaningful timing numbers only exist once the proxy is deployed and real network/model latency is in the picture (Step 12.3 residual).
- ~~STEP 6.1's detection service is built + stub-tested but NOT yet consumed: nothing fires detectState() on the button press, nothing renders pills, nothing feeds the detected state anywhere. Wiring is later Phase-6: 6.3 (pills UI + firing detection alongside translation on submit)...~~ PARTIALLY RESOLVED at Step 6.3 — detectState() now fires on every submit/refine, real pills render, session.statePills is written. Still parked: Step 6.5 (state feeds — directness auto-recommend/technique/tone/transparency actually CONSUMING that state).
- STEP 6.1's DETECTION_SYSTEM_PROMPT is a WORKING-BUT-PROVISIONAL classifier — Step 6.2 owns finalizing/tuning it for claude-haiku-4-5 and spelling out each state's system impact (Overwhelmed→directness L1, etc). Its real accuracy against real ADHD input is UNMEASURED (no live haiku call in this no-network sandbox; parked on the Step 12.3 deploy, same standing residual as every model call since 1.10). No detection test corpus exists (unlike translation's 50-case corpus); a future step may want one.
- STEP 6.1's schema allows a DimensionReading with confidence but the current prompt asks the model to fill it — the per-dimension confidence values are unverified against real model behavior (stub tests only). Confirm on deploy that haiku actually returns usable per-dimension confidence, or adjust the prompt/schema.
- STEP 6.2's STATE_IMPACTS is built and tested but only CONSUMED by tests — Step 6.5 (state feeds) is what actually applies impactsFor/recommendedDirectness/suggestedTechniques to directness/technique/answer-tone, and the transparency card (Step 8.2) shows them as the "why." The "X or Y" candidate reconciliation (run candidates through auto-detect's conflict/dep/≤4 logic) is explicitly 6.5's to implement — 6.2 only recorded the candidates.
- STEP 6.2's haiku-tuned prompt is unverified against a real claude-haiku-4-5 call (no-network sandbox) — whether Haiku reliably emits the exact JSON shape (esp. per-dimension confidence and a genuinely non-judgmental summary) is unmeasured, parked on the Step 12.3 deploy. No detection accuracy corpus exists (translation has one; detection does not) — a future step may want one to tune the prompt against real ADHD input.
- STEP 6.3's pills UI is typecheck+build verified only, not render-tested (repo-wide no-RTL choice) — a future browser-equipped step should confirm: pill colors actually read as purple/red/green/blue against the marble background, the corrector popover doesn't clip at the composer's edges, Adjust's open-all/close-all toggle feels right, and the panel's entrance/exit (appearing after detection resolves, disappearing on dismiss or a fresh submit) doesn't flash or feel jarring (CANON "no competing visual stimuli").
- STEP 6.3's correction control (PillCorrector) has no useFocusTrap (deliberate — see DECISIONS) — Tab-reachability of its buttons in natural DOM order is by construction, not verified in a real browser. A future keyboard-audit step (11.3) should confirm Tab/Enter/Escape all work as expected when one or several correctors are open at once.
- STEP 6.3's Adjust button behavior (open/close all correctors) is a documented interpretation, not confirmed against real source material beyond the screenshot showing the control exists — see DECISIONS. Revisit if a clearer spec surfaces.
- STEP 6.4 (Correction learning) must decide WHERE to hook into the correction flow: either extend CenterColumn's handleCorrectState (this step's display-only handler) to also record the correction, or have StateDetectionPanel's onCorrect prop carry more than a bare (dimension, value) pair if 6.4 needs more context (e.g. the pre-correction value, for a "was X, corrected to Y" audit log per CANON Feature 5's "audit log" language in similar features).

## STEPS
- [x] STEP 0 — File inventory and manifest
- [x] STEP 1.1 — Repo scaffold and stack lock
- [x] STEP 1.2 — Design token system
- [x] STEP 1.3 — Marble slab architecture
- [x] STEP 1.4 — Glass and marble primitives
- [x] STEP 1.5 — Layout shell and top bar
- [x] STEP 1.6 — Logo
- [x] STEP 1.7 — Dual-store state architecture
- [x] STEP 1.8 — Autosave and restore
- [x] STEP 1.9 — Keyboard framework
- [x] STEP 1.10 — API proxy and model registry  (ran after 2.1/2.2; live deploy parked)
- [x] STEP 2.1 — Gap taxonomy and prompt spec
- [x] STEP 2.2 — Translation engine
- [x] STEP 2.3 — Confidence gates and clarify flow
- [x] STEP 2.4 — Test corpus harness
- [x] STEP 3.1 — Six-dimension scorer
- [x] STEP 3.2 — Decision logic and override
- [x] STEP 3.3 — Low-confidence escalation
- [x] STEP 4.1 — Technique registry and matrix
- [x] STEP 4.2 — Scoring and stacking
- [x] STEP 4.3 — Composition engine
- [x] STEP 4.4 — Directness control
- [x] STEP 4.5 — Technique selector UI
- [x] STEP 5.0 — Input composer and control row
- [x] STEP 5.1 — Streaming display
- [x] STEP 5.2 — Pipeline orchestrator
- [x] STEP 5.3 — Error, retry, timeout
- [x] STEP 5.4 — Telemetry
- [x] STEP 6.1 — Detection architecture
- [x] STEP 6.2 — Four-dimension classifier prompt
- [x] STEP 6.3 — State pills UI
- [ ] STEP 6.4 — Correction learning
- [ ] STEP 6.5 — State feeds
- [ ] STEP 7.1 — File upload and limits
- [ ] STEP 7.2 — OCR pipeline
- [ ] STEP 7.3 — URL fetch
- [ ] STEP 7.4 — Variables
- [ ] STEP 7.5 — Context Snapshot panel
- [ ] STEP 8.1 — Feedback rating
- [ ] STEP 8.2 — Transparency card
- [ ] STEP 8.3 — Debate mode
- [ ] STEP 8.4 — Consensus and Synthesis
- [ ] STEP 8.5 — Download and export
- [ ] STEP 9.1 — Session lifecycle
- [ ] STEP 9.2 — Templates and saved prompts
- [ ] STEP 9.3 — Import system
- [ ] STEP 9.4 — Visibility toggle
- [ ] STEP 9.5 — Revolving-door accordions
- [ ] STEP 9.6 — Quick Tools grid
- [ ] STEP 9.7 — Left nav content
- [ ] STEP 10.1 — Pattern analysis engine
- [ ] STEP 10.2 — Rule refinement application
- [ ] STEP 11.1 — ADHD hard rules audit
- [ ] STEP 11.2 — Performance pass
- [ ] STEP 11.3 — Keyboard audit
- [ ] STEP 11.4 — Visual verification
- [ ] STEP 11.5 — Apply the audit fixes
- [ ] STEP 12.1 — Unit tests
- [ ] STEP 12.2 — End to end flows
- [ ] STEP 12.3 — Deploy

---

## STEP 0 NOTES

**File inventory complete.** All four source-of-truth docs present and consistent.

**What's here:**
- CANON.md, MATERIALS.md, PIPELINE.md, ROUTING.md (source of truth, verified aligned)
- FINDINGS.md, README.md (reference docs, no conflicts)
- Divergence_AI_App_Screenshot_V3.png (visual truth, authoritative)
- routing.js and tests.js (assumed present in repo, not attached; routing.js needs one string update)

**What's missing or clarified:**
- Three texture files (black marble background, two greys): location/source to be confirmed before Step 3.1
- Translation test corpus (31_0_translation_test_cases.md): optional, needed only if Step 2.4 is run

**Locked decisions verified:** All four are consistent across all files. No conflicts to resolve.

**Next:** Step 1.1 (Repo scaffold) can proceed immediately. No blockers.
