/* Pipeline feature UI (Step 5.2: the live center column — composer wired to
   the orchestrator, conversation area rendering the run). */
export { CenterColumn } from "./CenterColumn";
export { usePipelineRun, type ActivePipelineRun, type PipelineRunView } from "./usePipelineRun";
