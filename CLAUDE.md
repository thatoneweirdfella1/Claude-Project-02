# DIVERGENCE.AI governed continuation

Read and obey `START-HERE-DIVERGENCE.md` and `AGENTS.md`.

Use the authenticated GitHub API workflow when no executable checkout exists. External owner actions are deferred under `docs/layer-system/EXTERNAL-DEPENDENCY-DEFERRAL.md`; they block only the affected live proof and completion label, not safe later-layer implementation.

Use only the exact working branch and permissions recorded in the branch itself. Earlier layer branches, `build`, `frozen-implementation-v1`, and backups are read-only. Do not invent decisions, weaken tests, alter frozen visuals, merge, or deploy production.
