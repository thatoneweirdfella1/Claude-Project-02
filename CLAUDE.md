# DIVERGENCE.AI governed continuation

Read and obey `START-HERE-DIVERGENCE.md` and `AGENTS.md`. Work only on `horizontal-layer-5-implementation-v1`.

Use authenticated GitHub API Work mode when no local checkout exists. Upstash remains deferred and does not block safe deterministic Layer 5 work. Never use real funds, a real payment provider, external AI, secrets, merge, production, or an earlier layer branch.
