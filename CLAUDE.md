# DIVERGENCE.AI — Mandatory Claude Entry Rule

Before analyzing, planning, editing, running commands, or proposing changes, read and follow `docs/ai-control/00-READ-FIRST.md`.

Preserve `claude/remaining-second-pass-v1` as the untouched safety/layout branch. Make new task commits only on reusable staging branch `divergence/reliability-staging`; merge accepted work into `divergence/reliability-v1`. Do not create another branch. Do not replace or redesign the existing Divergence interface. Perform only the active task, log meaningful work and rationale, park extra ideas, and stop if repository or task authority is missing.

The machine-enforced course policy is `docs/ai-control/COURSE-CONTROL.json`. Run `scripts/ai-course-control.mjs` before accepting a checkpoint. Failure or unavailable enforcement means stop, not bypass.

This file is only an entrypoint. Canonical authority is in `docs/ai-control/`; do not create a competing instruction set.
