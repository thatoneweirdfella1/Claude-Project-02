# 2026-08-24 hybrid audit reconciliation

**Pre-correction verdict:** FAIL  
**Correction state:** APPLIED AND SELF-VALIDATED  
**Post-correction verdict:** PENDING INDEPENDENT AUDIT

This is a reconciliation record, not an independent `PASS`. The installer compared the supplied Haiku, Sonnet, Opus 4.8, and Opus 5 audit outputs against the governing repository artifacts and accepted findings only when the underlying evidence supported them.

## Required corrections accepted

1. Use the canonical ledger vocabulary; `PASS`/`FAIL` are gate verdicts, not layer-row completion states.
2. Require every applicable row to be `PROVEN`; permit `N/A AT THIS DEPTH` only through exact per-layer/per-ID clauses.
3. Retire the standalone sequential `FIX ALL` command in favor of coherent horizontal batches.
4. Reconcile source checkpoint, continuation branch, frozen comparison, and protected baseline identities.
5. Make tests and workflow results layer-scoped, assertion-scoped, and environment-scoped.
6. Map all 48 workflow acceptance handles into the ledger and validate all four registries bidirectionally.
7. Add regression demotion, failed-gate, recovery, and matching-deployment rules.
8. Start security, prompt-injection, accessibility, ADHD-friction, recovery, and migration proof at the layer where each risk becomes real.
9. Track `SPEC-LS-01` and `SPEC-FB-01` as explicit non-decision project-task blockers.
10. Make Layer 1 require the exact approved destination, no blank/router-error/fake-success state, and matching preview identity.
11. Preserve all 39 repair groups, 7 decisions, 48 workflows, 266 acceptance tests, and 218 permanent IDs without inventing product decisions.

## Findings rejected or narrowed

- No invented permanent IDs, feature-flag names, architecture artifacts, or product standards.
- No token-budget estimation gate; cost control belongs to actual execution/evidence rules.
- No claim that requirements without a repair ID are absent; permanent IDs remain the master denominator.
- No claim that workflows were absent; the real defect was their 48 unmapped acceptance handles.
- No broad removal of the desktop requirement; `SPEC-DS-01` remains in the denominator and is proven on its separate track.
- No invented Tier 1/Tier 2 product behavior or reopened settled decisions.

## Verification still required

A different account/session must run `INDEPENDENT-AUDIT-CHECKLIST.md` from a real checkout, preserve the preflight certificate, and issue the post-correction `PASS` or `FAIL`. Until then, no horizontal layer is complete and application edits remain denied.
