# Decision Log

| ID | Decision | Authority/status | Rationale | Supersession rule |
|---|---|---|---|---|
| D-001 | Continue in the existing `thatoneweirdfella1/Claude-Project-02` repository; do not create a separate repository. | User-directed; active | The reliability layer belongs to the existing Divergence product and must retain its real interface and history. | Only the user may authorize another repository explicitly. |
| D-002 | Preserve and build from the existing Divergence layout and supplied three-part visual baseline. | User-directed; active | The user wants that layout, not an AI-invented replacement. | Only an explicit user-approved visual change may supersede a specified part. |
| D-003 | Exactly one new long-lived working branch is authorized: `divergence/reliability-v1`. No AI may create another branch. | User-directed; fulfilled and active | Existing branch proliferation is already excessive. One provider-neutral branch holds the entire reliability effort. | Only a new explicit user instruction may authorize another branch. |
| D-004 | The user's chosen Vercel site maps to safety/layout branch `claude/remaining-second-pass-v1` at deployed commit `10894f704a39b6c56a7fadfafb54275b82526c33`; preserve it without writes. | User-directed and verified from Vercel/GitHub metadata; active | It is the known-good recovery source and visual baseline. | Only the user may select another baseline; never silently move it. |
| D-009 | Create `divergence/reliability-v1` from exact commit `10894f704a39b6c56a7fadfafb54275b82526c33` and perform all new reliability work there. | User explicitly authorized; fulfilled | Inherits the desired layout while isolating new work from the safety branch. | Do not create a replacement branch; repair or revert within this one unless the user says otherwise. |
| D-005 | Keep one active task; park every out-of-scope idea without implementing it. | User goal translated into control rule; active | Prevents useful discoveries from being lost without allowing AI scope expansion. | Only explicit user promotion can activate a parked item. |
| D-006 | Use separate canonical files for authority, current task, chronological work, decisions, evidence, handoff, and parked ideas. | Execution design; active | These are different kinds of state; separating them keeps resumption fast while avoiding one unreadable log. | Consolidation requires proof that no information or control boundary is lost. |
| D-007 | Root AI-specific files are short pointers to one canonical control set. | Execution design; active | Claude, Codex, and GitHub tooling recognize different entry files; one underlying authority prevents contradictory copies. | New entrypoints may only point to the same canonical files. |
| D-008 | F0 is the only active task and remains design-only; a separate AI audits it before follow-on work. | Existing accepted sequence; active | Preserves prerequisite order and prevents self-declared completion. | Change only through an explicit user decision recorded here. |
| D-010 | Pause F0 and make G0, the AI Course-Control Gate, the only active task on `divergence/reliability-v1`. | User explicitly directed; active | The user requires enforcement that keeps AI work on course without continuous ADHD-heavy micromanagement. | F0 may become active only after G0's stated exit requirements and explicit user approval. |
| D-011 | Create exactly one permanent reusable staging branch, `divergence/reliability-staging`; protect `divergence/reliability-v1` as integration and never create per-task branches. | User explicitly authorized with “do it”; active; supersedes the single-writable-branch portions of D-003 and D-009 | GitHub must test a commit before allowing it into the protected integration branch; one reused staging branch provides that path without branch proliferation. | Only a later explicit user instruction may authorize another branch or replace this flow. |
| D-012 | Install the exact supplied `02-MASTER-SYSTEM-AND-REQUIREMENT-BLUEPRINT.md` at repository root as the canonical master planning/control blueprint, without redesigning or shortening it; complete only this bounded B0 task and keep F0 and application implementation locked. | User explicitly directed; fulfilled by B0 self-check | Gives every future AI one complete system-and-requirement source while preserving the user's branch, scope, and sequencing controls. | Only a later explicit user instruction may replace the canonical blueprint, alter its authority, or activate F0. |

Unresolved items remain in `CURRENT-TASK.md`, the F0 assignment, or `PARKING-LOT.md`; do not convert an unresolved item into a decision.

**Status correction:** D-008's statement that F0 was the active task is historical and was explicitly superseded by D-010. G0 remains the only active task until its exit is recorded; F0 remains locked pending explicit user transition approval.

**Later status correction:** D-010's active-G0 state became historical after G0 closed. The user's B0 instruction made canonical-blueprint installation the single bounded active task; B0 is complete by self-check. F0 is the exact next unfinished task but remains locked pending a separate explicit user activation instruction.

## Later decisions

| ID | Decision | Authority/status | Rationale | Supersession rule |
|---|---|---|---|---|
| D-013 | Activate F0, Foundation Contract and Interface Skeleton, as the only active task on `divergence/reliability-staging`; stop after its design package, records, checks, and commit without starting an independent audit, any S02/S03/S18/S20 package, F1, or application implementation. | User explicitly replied `authorized` after receiving the exact F0 lock/next-task statement; active | Satisfies the last F0 unlock prerequisite while preserving the established one-task sequence and branch boundary. | Only a later explicit user instruction may activate the independent F0 audit or any follow-on task. |
| D-014 | Make G1, Continuity, Acceptance, and Contamination Controls, the only active task before the independent F0 audit; add continuous resumable checkpoints, audit/dependency blocking, plain-language next-AI notification, protected acceptance, lineage/contamination containment, and executable hostile tests while keeping F0 and all product-system work locked. | User explicitly directed the AI to begin the complete pre-work control list with checkpoints so another AI can finish after a usage interruption; active | Prevents the user from having to remember or police task handoffs and prevents an unaudited upstream error from silently authorizing downstream work. | Only a later explicit user instruction may change G1 scope or activate work beyond its verified exit. |
| D-015 | After preserving G1-E, activate `G2 — Maximum-Feasible Automation and Non-Bypass Hardening` as the sole correction task; implement every useful in-architecture safeguard and require a different AI to audit all authored changes. | User explicitly directed; active | G1-E found load-bearing stale-state and same-change trust-boundary defects. | G2 cannot independently approve itself; only later independent evidence and user-authorized acceptance can advance dependents. |
| D-016 | Routine human GitHub approval, a second GitHub account, and manual audit/task routing are prohibited dependencies for normal operation. A candidate-independent controller must automatically validate, assign an authenticated independent AI audit, create and reassign corrections, re-audit, accept verified work, and unlock satisfied dependents. The user is interrupted only for a material product decision absent or contradictory in existing authority. | User explicitly corrected the autonomy requirement on 2026-09-08; active; supersedes the routine-user-acceptance portion of D-015 and any lower-authority conflicting record | Requiring the user to remember or perform routine approval defeats the ADHD-support and autonomy outcome. | Only a later explicit user instruction may restore routine human approval. |
| D-017 | Retain Vercel project `prj_2tCUMX2TrV6ZIdKQnGDmOXZDPST3` and treat its stable production target as permanent **non-product controller infrastructure**. It remains isolated, unlinked to Git, controller-only, and must never replace or modify the `claude-project-02` product deployment. | User told Codex to continue the G3-A task after Codex explicitly recommended retaining the isolated target; active; supersedes only the preview-only environment restriction in CL-0049–CL-0051 | Vercel production identifies the stable environment within this separate project. A permanent webhook and required-check authority needs a stable endpoint; a disposable preview URL is a weaker trust and continuity boundary. | Only the user may authorize linking this controller project to product Git, placing product code in it, or replacing the product deployment. |
| D-018 | Bootstrap durable state only from versioned migration package `2.0`, atomically and create-only; retain prior failures, audits, decisions, evidence, lineage, and dependency locks. | User's 2026-09-09 live-bootstrap instruction; active | The prior seed contained only a fresh G2 task and would lose authoritative retained history. Redis initialization must fail closed and never replace existing state. | A later migration may supersede this only if it proves lossless forward migration and remains create-only against an existing state key. |

**Current status correction:** D-013 supersedes only the historical active-task portions of D-008, D-010, and D-012. Their sequencing, branch, safety, and scope restrictions remain active. F0 is authorized; no later task is authorized.

**Latest status correction:** D-014 supersedes D-013 only as the active task. F0 remains complete by self-check and frozen pending independent audit. G1 is now the sole authorized task; independent F0 audit, S02/S03/S18/S20, F1, and implementation remain locked.

**G2 status correction:** G1-E completed with a retained Failed verdict. D-015 makes G2 the sole active correction task. G1 remains failed/unaccepted and all dependent work remains locked.
